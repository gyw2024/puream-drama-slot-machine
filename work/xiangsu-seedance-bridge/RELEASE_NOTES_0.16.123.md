# 纯梦短剧老虎机 0.16.123

## 修复

- 修复上传剧本角色 ID 重排后，镜头对白虽然已按姓名纠正，但不可变对白账本在持久化时又优先写回旧 ID，导致 D001 起说话人完整性校验复发的问题。
- 不可变对白账本和镜头对白统一以原稿说话人姓名为事实源；旧 `speakerId` 仅在原稿确实缺少姓名时兼容使用。

## 继承

- 继承 0.16.122 的资产提示词准入修复，以及 0.16.121 的启动、页面切换和大资产库性能优化。
