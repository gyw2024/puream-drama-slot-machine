# 纯梦短剧老虎机 0.16.116

## 中文多模态时间轴根修复版

- 所有 H3 分镜视频提示词统一为 `integrated_multimodal_description（多模态综合描述）` 中文执行合同；提示词确认页、单镜编辑页、项目缓存和真正提交给 H3 的文本使用同一份原文。
- 每条提示词完整列出参考图/场景四视图/人物/服装/商品/音色绑定，并按 `[镜头 N]` 与 `00:00.000—00:00.000` 毫秒时间轴排列动作、机位、对白、语气、表情、朝向、说话嘴型和闭口听者。
- 无显式时间码的对白先匹配原稿中“某角色说出/回应/回答”的分镜证据，再按自然语速分配窗口；群众鼓掌、物品特写等无对白镜头不会再被错误塞入另一角色台词。
- 人物基础身份图不再覆盖专用换装图；带货收口的 `directToViewer` 会落实为脸、眼睛和上半身正对镜头；未上传参考图的静默人物不再泄漏进上游人物表。
- 旧英文六段式和旧缓存会在本地自动重新编译，不调用文本模型、不产生额外文案费用；用户在弹窗看到并编辑的就是最终执行稿。
- 修复语气字段含内嵌中文引号（如重读“活得体面”）时被旧校验器误判为缺少表演合同的问题。

## 验证

- 全量源码回归：`722/722`。
- 提示词确认 UI：7 组视口/缩放、交互 `8/8`，无严重可访问性问题；长提示词在等高等宽卡片内部滚动。
- 构建态、安装态及唯一一次真实 H3 视频任务的 taskId、计费、下载、探测和全帧解码证据随任务审计文件保存。
- 真实 H3：taskId `cmti3q8u3020qv6kp8cfahrdw`，只提交 `1` 次，回执结算 `1.22` 元，10.13 秒竖屏 MP4 完整解码通过。
- Windows 安装包：126858803 bytes，SHA-256 `5D7A0E50023F7B233A4EDAC02676CAA48F954373DE34E40E60CA31EA6E858047`。
- 未更新官网，未使用桌面自动化。
