# 0.13.38

- 所有视频提交统一生成不超过上游限制的执行提示词，保留对白、说话人、语气、听者反应和音色绑定，不再显示 `PROMPT_TOO_LONG`。
- 自动修复人物孤儿引用并从资产/分镜/视频断点继续；后续发现缺资产或缺分镜时自动回补。
- 上传剧本的分块占位场景统一归并，不再生成“用户原稿场景N”。
- 历史分镜永久保留并可恢复为当前版本；自动修复只追加新版本，不替用户作废旧成果。
- 审核蓝图关闭时跳过视频与合成质检；开启时保留修复或忽略继续。
- 桌面端清除内部算力实现名称，统一显示“云端算力节点”。
