# 0.13.22

- 官网授权用户不再使用本地假令牌；激活后必须取得短剧管理后台签发的真实会话与生图、视频并发额度。
- 一键流程、分阶段生成、跨项目生成和旧视频直提入口统一申请服务端租约，不允许绕过管理后台并发限制。
- 管理后台并发服务不可用时，新的生图和视频任务失败关闭，不再使用离线本地计数继续生成。
- 旧版 `website:*` 授权状态启动后自动迁移为真实服务端会话，保留授权码、项目和用户设置。
- 租约申请支持同一任务幂等重试，避免网络响应未知时重复占用并发。
