# 纯梦短剧老虎机 0.16.133

## 修复

- 修复上游 H3 尚未收到创建请求时，工作台却显示“后端生成中”并持续转圈的问题。
- 本地参考图上传、鉴权和提交准备改为独立状态；只有上游返回有效 `taskId` 后才进入生成队列。
- 暂停发生在上传结束和 HTTP 提交之间时，提交被硬拦截，不会创建上游任务，也不会误记提交次数。
- HTTP 成功但缺失 `taskId` 时保留原幂等键并进入结果核实，不允许盲目重提。
- 上游创建请求前失败统一记为 `not_charged`；实际跨越请求边界后才允许记为待结算。

## 真实项目恢复

- 九宝茶导入项目中的 16 条无上游任务记录已校正为“上游未创建”，taskId 数量为 0，远端任务索引匹配数为 0，费用状态为未扣费。
- 每个分镜仍严格最多创建一个上游任务；本地准备失败可沿用同一任务身份继续，不会新建第二条本地任务。

