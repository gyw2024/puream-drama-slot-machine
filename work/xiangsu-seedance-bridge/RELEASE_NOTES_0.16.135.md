# 纯梦短剧老虎机 0.16.135

- 保持管理员授权的 16 路分镜提交，并将参考素材上传从 3 路恢复为 16 路；同内容引用仍由共享缓存只上传一次。
- 资产包在任何上传或上游请求前完成官方 H3 模式、镜头锚点和 10 秒时长合同检查；不兼容包本地立即停止，明确显示未创建任务、未扣费。
- 主进程重启后，旧上传/提交调用不再保留 20 分钟假活跃状态：上传前中断立即标记未创建，已跨 HTTP 边界但未拿到 taskId 的记录标记为待恢复，并只复用原幂等键。
- 上传重试耗尽后进入可继续的暂停态，不再一直显示“正在上传”。
