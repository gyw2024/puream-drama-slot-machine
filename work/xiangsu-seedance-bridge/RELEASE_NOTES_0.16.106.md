# 纯梦短剧老虎机 0.16.106

- 修复 H3“人物、场景、物品、音色直接参考”模式的核心编译路径：正式提交统一使用 MiniMax H3 官方六段式参考协议，不再把内部自研提示词方言直接发给上游。
- 人物使用稳定 `<Subject N> (S1/S2)` 身份，说话内容只在 `<d>[Chinese] ...</d>` 中出现一次；`<Audio N>` 仅绑定对应人物音色，换人时同步切换镜头和说话嘴型。
- 场景、角色、商品、服装、道具全部转换成明确的 `<Picture N>` / `<Subject N>` 依赖，清除详细执行段中的 C/P/W/SC 内部编号，避免上游无法理解引用关系。
- 动作、表情、语气、监听者、起止状态和后半段静默收束均保留在同一时间轴；服装归属、人物间动作归属和 30ml 独立袋尺度改为可见、明确的物理描述。
- 最终视频提示词采用正向的“完整摄影画面”输出合同，不向模型注入字幕、标题、水印等可见文字概念。
- 兼容旧项目的已确认提示词与恢复记录；已提交 H3 任务只轮询原 taskId，不会因查询波动重复创建付费任务。
- 全量 652/652 回归通过。真实测试完成 62 秒资产直投短剧链路，并对修复后双人对白分镜执行一次唯一付费复测；累计上游实际成本 ¥14.50，未超过 ¥15 上限。
- 本版本仅构建本机安装包并静默更新本机，不发布官网。
