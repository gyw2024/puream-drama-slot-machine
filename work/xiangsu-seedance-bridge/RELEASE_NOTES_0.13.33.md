# 纯梦短剧老虎机 0.13.33

发布日期：2026-08-14

## 本次修复

- 三种 AI 剧本模式正式放入“制作策略”：完整制作稿、简易对白稿、秒级分镜成片稿；新建 AI 项目与未确认的历史项目必须先选择，三种模式都可直接下载示例。
- 审核蓝图改成端到端显式开启。未勾选总开关、模块或明细时，对应审核、拦截、回滚和自动返修绝不运行；清除 HTML 启动模板中的旧默认勾选。
- 人物视频与资产依赖按本地像塑/云端 H3 区分：本地像塑不再创建 H3 人物视频和音色前置任务；云端缺独立人物图时自动补齐，远端暂停只保留待续跑任务，不阻断场景、道具和分镜图。
- 保留 AutoDL/上游返回的真实错误码，`AUTODL_QUARANTINED`、远端任务未决和音色依赖改为可恢复状态，不再误报整批失败。
- 文本中转补齐 Windows Electron `net::ERR_FAILED` 响应前故障恢复；使用同一幂等请求号切换网络栈或后台重连，避免重复计费。

## 验证口径

- 剧本模式、审核显式开关、两类视频引擎、四种视频模式、分步/一键、AI/上传剧本均进入最短链路矩阵。
- 官网生产文本与余额路由完成无付费健康探针；真实文本探针只使用固定幂等号与最小 32-token 输出。
- 最终源码自动回归 126/126 通过；20 秒最短链路覆盖两种引擎、四种视频模式和分步/一键入口。
- 打包态高强度 UI 审查通过：1024×720、1280×800、1440×900、1920×1080、200% 缩放均无横向溢出、控件重叠或严重无障碍问题；审核关闭/开启、三种剧本模式和跨项目资产库均完成真实交互复验。
- 本机已静默升级并通过安装态审查，文件版本 `0.13.33`、产品版本 `0.13.33.0`，隔离启动没有自动提交任何付费任务。
- 安装包 125226321 字节，SHA-256 `CF20F534FC2D02A0ED1BA56852C9D831F998930944160605BD1BDE31476AA963`。
- 真实最小文本探针被官网接收，但上游 300 秒未完成；官网最终按 0 token、0 元失败结算，临时冻结 0.01 元已全额释放、冻结余额归零。正式写作流程在 60 秒内本地补齐并继续，不把该上游长等待显示为用户失败。
