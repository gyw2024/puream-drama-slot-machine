# 纯梦短剧老虎机 0.16.125

## 修复

- 新增分镜视频严格一次提交合同：每个确定性 H3 生成单元最多提交一次，已有任务只查询和下载，失败不自动重投。
- 内部连续生成片段不再冒充完整分镜，不会被自动选中、确认、显示为资产或加入全局资产库；只有全部计划片段齐备后才在本地合成完整分镜。
- 修复音色并发标准化、人物图确认误伤独立音色、运行态字段污染提示词确认缓存的问题。
- 修复道具 ID 过宽匹配、项目候选与全局资产库缺少反向链接、并发图片任务号碰撞串图的问题。
- 继承人物/物品资产准入、外貌提示词、40 个不同性别年龄音色、分页渲染和启动性能优化。

## 真实链路边界

- 九宝茶项目完成 42 镜零付费预检，计划 69 个 H3 生成单元；实际提交记录 68 个且每个最多一次。
- 其中 66 个单元完成、2 个单元上游终止，最终得到 40/42 条完整分镜。按严格一次规则未重投失败单元，也未伪造最终成片。
