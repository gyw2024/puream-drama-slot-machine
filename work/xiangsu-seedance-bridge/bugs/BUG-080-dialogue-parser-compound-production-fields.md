# BUG-080

- 任务：`TASK-20260822-DRAMA-DIALOGUE-PARSER-087`
- 影响级别：S2
- 复杂度：C2
- 目标版本：0.16.87
- 现象：AI 标准化后的制作稿若使用 `分镜名称：`、`镜头时长：`、`画面与动作：` 等复合字段，原稿对白账本把字段名登记为 D001-D003 的说话人，随后因人物资产中没有这些假角色而以 `SCRIPT_DIALOGUE_BINDING_INVALID` 中断。
- 深层根因：系统先让 AI 将任意上传文本改为标准制作稿，再由本地解析器读取；同时又用本地对白账本保护逐字对白。旧解析器使用固定字段黑名单，只认识 `镜头`、`时长`、`画面`、`动作` 等单字段，不认识模型常见的复合字段。制作元数据和对白共用冒号语法，错误条目进入最高优先级事实账本后，正确的 AI 分析也会被脏账本反向否决。
- 修复：在解析边界增加可组合的制作字段语法；先提取 `对白/台词/旁白` 容器中的真实尾部，再过滤制作字段；说话人解析和最终入账双层拒绝制作字段；绑定历史项目账本前本地清洗旧污染项，不重传、不重新调用模型。
- 防复发：新增截图原句、同行内嵌对白、历史污染账本续跑、完整 `自由稿 → AI 标准稿 → 分析 → 资产阶段` 四类离线回归。
- 验证：聚焦测试 20/20；新增完整工作流测试 5/5；最终全量测试 460/460。NSIS 安装包构建成功并以退出码 0 静默覆盖本机原桌面版。安装后直接读取 `app.asar`：版本 0.16.87、复合字段语法与历史清洗代码均存在，截图原句只返回 `D001 母亲：你先听我说完。`；16 类跨厂商字段名矩阵均不进入对白账本。
- 安装包 SHA-256：`C7CDD96F8B5AF3B46086091D42DAB7239E0F73B077AE9BB0CF3D39E936ADB7C9`
- 安装后 app.asar SHA-256：`E04C5F93DF34914B0FD6688E35BD483236278373012ED3C847A46F709253C621`
- 回滚基线：`.codex_backups/baselines/TASK-20260822-DRAMA-DIALOGUE-PARSER-087`
- 状态：已修复、已测试、已打包、已静默安装并验证。
