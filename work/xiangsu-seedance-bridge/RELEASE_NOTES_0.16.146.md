# 纯梦短剧老虎机 0.16.146

- 修复 70 段完整成片在 Windows 上因 FFmpeg 过滤图进入命令行而触发的 `spawn ENAMETOOLONG`，改为临时过滤脚本传入。
- 六种 H3 制作模式统一继承开场 30 秒剧情清晰、成片零对白分镜禁止、三层终审与对白优先约束。
- 资产包新增“完整剧本—源对白台账—分镜对白”三方逐句、逐说话人一致性校验。
- 提示词保留语气、声线弧线、表情、动作、站位、朝向、视线和闭口反应，禁止对白标签外的隐含发声。
- 修复当前分镜误继承下一分镜动作的问题，并按对白字数、标点、表演停顿与动作时间函数复算时长。
- 用户数据只读升级审计忽略 Chromium 的运行时 LevelDB 锁文件，仍逐字节保护真实项目、资产、配置与数据库。
