# 纯梦短剧老虎机 0.13.26

- 修复新建项目的人物形象库为空：侧栏现在按需读取本机全局独立资产库，历史项目已确认的人物图可跨项目浏览和绑定。
- 修复选题模型返回有效 JSON 数组仍被判定缺少 `topics`：兼容根数组、常见别名和多层中转包装，同时保持剧本、分镜等其他 JSON 合同严格不变。
- 已保存的有效选题原始回复可在本地直接恢复，不会为同一回复再次提交上游或新增文本费用。
- 修正结构解析失败后选题状态残留为“生成中”的问题，失败状态现在可正确恢复和重试。
