# 0.13.29 热修说明

- 修复写剧本分段并发导致的文本连接失败：同一项目改为受控队列，未返回正文且未计费的网络波动会使用同一逻辑请求在后台自动恢复。
- 已完成的剧本段持续保存；部分正文或结算回执已返回时禁止盲目重发，避免重复扣费。
- 顶栏新增官网权威余额显示与微信充值入口，支付到账后自动刷新全局余额。
- 三种剧本格式均新增可下载示例：完整制作稿、简易对白稿、秒级分镜成片稿。
- 新增 `BUG_HISTORY.md`，永久记录历史用户问题、根因、修复版本与回归检查。
