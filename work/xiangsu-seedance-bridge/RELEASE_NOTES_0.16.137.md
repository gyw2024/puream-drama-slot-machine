# 纯梦短剧老虎机 0.16.137

- 删除资产包对“镜头锚点图”的错误依赖；直接使用包内锁定顺序的人物、场景、核心物品和商品原图。
- 多图无参考音频固定走海螺 H3 官方 `reference_to_video`，每镜支持 1–15 秒，不再擅自切成首尾帧模式。
- 官网确认的管理员账户不再受客户端批次或服务端租约数字并发上限；每个任务仍独立鉴权、保留幂等键与每镜最多一次提交保护。
- 普通账户的套餐并发和设备绑定保持不变。
- 参考素材继续按内容缓存，相同文件只上传一次；不同逻辑图片槽位及 Picture 编号不再因内容去重而丢失。
- 历史上游任务未创建且未扣费的记录改为“已暂停，可继续”，不再误报为生成失败或占用生成队列。
