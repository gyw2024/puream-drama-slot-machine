# 纯梦短剧老虎机 0.16.94

## 全流程根修复

- 海螺 H3 最终提示词改为对白优先：精确中文对白紧跟生产头，移除可能被模型误读成台词的英文 `speech_boundary` 自指令。
- 提示词规范和导演计划同步升版，历史缓存会自动失效，旧问题提示词不会继续复用。
- 用户主动重抽分镜视频时必定创建一个新候选；普通断点续跑仍复用原 taskId，避免重复付费。
- 生成任务可持续运行 20 分钟以上；单次只读状态连接改为 60 秒可替换查询，坏连接不会冻结整个任务，后续仍只查询原 taskId。
- 修复关闭蓝图后的场景、分镜图和分镜视频在全流程内部重复进入提示词确认的问题。
- 修复视频元数据清理把本地文件夹名中的 `prompt` / `workflow` 误判为上游元数据的问题。

## 真实验收

- 使用“书籍 晨与夜”真实商品参数完成剧本、提示词确认、人物、场景、分镜合图、音色、两段 H3 视频和最终合成。
- 修复后 S01 精确转写为“人到后来，才懂得告别也是生命的一部分”，不再出现开头英文误读。
- 完整成片 14.7 秒，H.264 + AAC，全量解码通过；两句对白完整、顺序正确，画面无额外字幕。
