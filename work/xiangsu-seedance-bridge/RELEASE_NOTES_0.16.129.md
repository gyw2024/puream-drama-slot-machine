# 纯梦短剧老虎机 0.16.129

- 分镜时长改为先按逐字对白、标点、情绪语速和必须完成的物理动作计算，再生成供应商时长。
- 新增确定性正常语速复算；窗口不足时只允许加长或按完整句、完整动作边界拆镜，禁止加速、吞字和截断。
- 接吻、拥抱、入退场、走位、跪下、踹开、扇耳光和物件交接使用独立动作时钟；相机切换不会重新执行同一动作。
- 只有严格的剧情首帧加尾帧使用 `image_to_video`；人物、场景、分镜合图及镜头锚点统一使用 `reference_to_video`。
- Codex 资产包升级为 v2 严格校验：对白归属、唯一口型、动作覆盖、英文控制词、无字幕、无参考音频和 1–9 张整段参考图均在导入前失败关闭。
- 九宝茶完整资产包已按新合同重编译并通过零提交导入验收。
