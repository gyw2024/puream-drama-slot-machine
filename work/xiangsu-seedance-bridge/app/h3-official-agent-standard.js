'use strict';
// Normative source is the provider documentation, not a prose-regex whitelist.
const SOURCES = [
  'https://huggingface.co/MiniMaxAI/MiniMax-H3/raw/main/docs/VIDEO_PROMPT_WRITING_GUIDE_ref_en.md',
  'https://huggingface.co/MiniMaxAI/MiniMax-H3/raw/main/docs/VIDEO_PROMPT_WRITING_GUIDE_base_en.md'
];
const PROTOCOL = `HAILUO OFFICIAL PROMPT AUTHORITY (verified 2026-09-14):
The applicable official MiniMax-H3 reference/base guide is mandatory. Select it from the actual image/video/audio payload, not the application's mode label. Full-reference prompts use these six English sections in order: subject_definitions, summary, retention_analysis, detailed_description, overall_soundscape, non_diegetic_music. Base first/last-frame or image-to-video prompts instead use the applicable official base template and its frame-alignment opening. Do not force the six-section reference template onto a base request.
Keep stable <Subject N>, <Picture N>, <Video N>, <Audio N> identities consistent with the actual supplied manifest. A speaking person retains both <Subject N> and (Sx); Sx follows first distinct vocal appearance, independently of asset numbering. Dialogue remains in its original language inside <d>[Language] ...</d>. Preserve every source line, speaker, listener and chronological position; do not retype dialogue as an extra stress cue. Clearly distinguish visible speech, off-screen speech, and source-authorized recording playback.
English staging must name the physical actor, action, causal contact/result, camera framing/motion, and audible event. The first camera header is [Shot 1] without a cut timestamp; later real cuts use [Shot N] At MM:SS.mmm. Begin reference detailed_description with the applicable short style description. Visible-content retention types are fully_preserved, partially_preserved, attribute_transfer, weak_reference. Audio retention uses fully_copy, partially_copy, reference, weak_reference; select its actual role separately. select their scope from the actual reference relationship. Preserve intrinsic identity without freezing a reference picture's pose, holder, lighting or completed end-state over the entire shot. Summary task markers reflect actual reference generation, keyframe completion, continuation or audio-reference inputs.
The official reference guide normally targets 350-500 English words in detailed_description; dialogue completeness takes precedence in dialogue-dense material. Do not silently truncate speech or physical causality to meet a local word heuristic.
Official identity markers, dialogue tags, section names and template-specific opening are required. Natural English staging has no application-invented compulsory sentence such as 'faces ... and says exactly once'. That phrase may be used when accurate, but equivalent unambiguous official-style English is valid. Do not reject 'addresses', 'turns toward', or a clear off-screen description merely because it differs from a local example. Base voice-over uses the official phrase says in an off-screen voiceover and states after the dialogue that the corresponding on-screen character's lips remain closed. This specific official requirement is not an application-invented universal phrasing rule. Judge the actual meaning against the applicable official guide and source evidence.
You, the Agent, own content review: dialogue completeness/ownership, timing feasibility, stance, emotion, tone, gaze, contact, reference fidelity, commerce and continuity. When a material contradiction exists, identify the exact affected item and author its smallest repair instruction, preserving all unaffected accepted content. Verify the repaired item. No speculative or cosmetic finding may restart a whole film. A software transport or schema check is not content approval.\nOfficial sources: ${SOURCES.join('\n')}`;
const INSTRUCTION = require('./production-content-requirements').INSTRUCTION+'\n'+PROTOCOL;
module.exports = {SOURCES, INSTRUCTION, PROTOCOL};
