# 纯梦短剧老虎机 0.16.102

- 用户上传任意格式剧本统一执行“AI 无损标准化 → 标准稿解析”，人物、场景、对白、动作和时长不再回流读取原始混合文本。
- 已付费分析断点可在本地移除“第一幕”等假人物、复用正确结果并继续，不因参考时长重新估算而重复提交。
- 分镜视频按单一说话人、单一口型归属和单一机位原子生成；供应商不再接收整张多格分镜板，只接收独立 9:16 帧。
- 最终拼接统一方形像素 9:16 画布并补齐静音音轨，避免白边、拉伸和末尾缺声。
- 上传标准稿的模型自审计仅计数出错时直接使用完整正文，不为修正计数重复付费请求。

本版本仅更新本机和安装包，未发布官网。
