# 纯梦短剧老虎机 0.16.134

## 根因修复

- 资产包视频提交前先验证官网 H3 官方能力，不再先上传几十秒再等待上游返回 400。
- 无音频资产包必须为每个分镜提供一张已经合成人物、场景、服装和商品的 `shot_anchor` 镜头锚点图；分散的人物/场景资产板会在本地立即停止，并明确显示未创建上游任务、未扣费。
- 合法的单张 `shot_anchor` 使用官网无音频图生视频传输合同提交；在官网内部映射为同一画面的首尾约束，不改变工作台的“Codex 资产包直抽”制作模式。
- 官网无音频图生视频单任务超过 10 秒时本地立即停止，要求资产包提前拆镜，禁止截断对白。
- 独立分镜提交和参考素材上传均恢复为授权的 16 路；上传前的合同预检先在本地一次性拦截无效包，合法任务才进入并发队列。
- 参考素材按内容共享上传结果，同一批次的重复人物、场景与商品引用只上传一次；不再以降低并发掩盖无效请求和主进程反复序列化造成的卡顿。
- 本地上传重试结束后任务进入“已暂停/未创建上游任务”，不再伪装为仍在上传；继续任务沿用稳定请求标识恢复。

## 当前项目恢复

- 九宝茶项目累计 30 条无 taskId 本地记录已统一校正为 `not_created/not_charged`；远端任务索引匹配为 0。
