# 纯梦短剧老虎机 Bug 台账

本台账记录已确认的用户问题、根因、首次修复版本和必须持续执行的回归检查。状态只有通过源码测试与安装包审计后才能标记为“已验证”。

| 编号 | 用户现象 | 根因 | 首次修复版本 | 回归检查 | 当前状态 |
| --- | --- | --- | --- | --- | --- |
| BUG-001 | 一键生成选题被中止或 JSON 结构不匹配 | 中转返回包裹层与客户端严格结构解析不兼容 | 0.13.28 | `topic-relay-regression.test.js` | 已验证 |
| BUG-002 | 上传长剧本后解析、拆镜在 5 分钟停止 | 客户端存在固定文本期限且上游内容未分块 | 0.13.28 | `script-format-no-deadline-regression.test.js`、`uploaded-analysis-resume-regression.test.js` | 已验证 |
| BUG-003 | 审核蓝图关闭后仍编译、打回或拦截 | 审核开关只影响界面，未贯穿编译与返修入口 | 0.13.28 | `blueprint-off-global-bypass.test.js`、`blueprint-off-prompt-compiler-regression.test.js` | 已验证 |
| BUG-004 | 逐秒合图仍被尾帧或公网参考图拦截 | 模式矩阵复用了首尾帧连续性合同 | 0.13.27 | `twenty-second-full-matrix.test.js` | 已验证 |
| BUG-005 | 上一阶段结束后视频自动生成，按钮仍显示不可用 | 任务恢复与用户触发状态混用 | 0.13.27 | `adversarial-state.test.js` | 已验证 |
| BUG-006 | 手改分镜提示词后“再抽一次”没有运行 | 编辑态被蓝图校验与未提交弹层拦截 | 0.13.28 | `blueprint-off-global-bypass.test.js` | 已验证 |
| BUG-007 | 新建项目不能并行、切换项目卡死、历史项目无法删除 | 全局 busy 状态代替了项目级任务状态，删除缺少回收区 | 0.13.28 | `adversarial-state.test.js` | 已验证 |
| BUG-008 | 用户上传剧本却仍按设置时长强行改写 | 上传模式与 AI 全生成共用时长合同 | 0.13.28 | `duration-contract-regression.test.js` | 已验证 |
| BUG-009 | 人物资产库新项目为空 | 可复用资产被错误限定在当前项目 | 0.13.28 | `adversarial-state.test.js` | 已验证 |
| BUG-010 | 视频提示词缺少说话人、语气、听者和完整对白 | 提示词编译未把对白账本设为最高优先级 | 0.13.28 | `hailuo-prompt-contract.test.js`、`prompt-matrix-dialogue-regression.test.js` | 已验证 |
| BUG-011 | 长时间写剧本但文本扣费显示为 0 | SSE 结算回执没有同步到项目文案账本 | 0.13.28 | `timed-storyboard-prompt-cost-regression.test.js` | 已验证 |
| BUG-012 | 文本阶段显示上游原始 JSON/额度错误 | 服务端原始异常未经公开错误映射 | 0.13.28 | `topic-relay-regression.test.js` | 已验证 |
| BUG-013 | 充值后写剧本仍在约 5 分钟处显示连接失败 | 同项目同时发出 3 个大文本流；网络中断被标记为禁止重试且整阶段失败 | 0.13.29 | `text-recovery-regression.test.js`、`manual-entry-fast-script.test.js`、安装包审计、官网 r79 文本接口公网探针 | 已验证 |
| BUG-014 | 软件内无法查看官网余额或直接充值 | 桌面端未接入官网统一钱包与微信充值订单 | 0.13.29 | `wallet-recharge-contract.test.js`、官网钱包专项 4/4、官网 r79 余额接口公网探针、安装包审计 | 已验证 |
| BUG-015 | 已准备三种剧本示例，但历史项目和上传剧本用户看不到 | 示例入口只存在于首次 AI 写作格式弹窗，弹窗确认后不再出现 | 0.13.30 | `script-format-no-deadline-regression.test.js`、打包态与安装态审计 | 已验证 |
| BUG-016 | 软件内充值门槛与运营要求不一致 | 桌面端直接沿用了官网 30 元最低充值规则 | 0.13.30 | `wallet-recharge-contract.test.js`、充值弹窗打包态与安装态审计 | 已验证 |
| BUG-017 | 写文案运行一小时后提示“应拆成 4 个生成单元，实际返回 0 个” | 文本请求没有截止时间；上传剧本拆镜并发超过官网两路上限；解析未兼容常见包裹层；上游返回说明文字时客户端重复请求后仍整阶段失败 | 0.13.31 | `direct-fast-script.test.js`、`uploaded-analysis-resume-regression.test.js`、`uploaded-script-local-fallback.test.js`、全量测试、打包态与安装态审计 | 已验证 |
| BUG-018 | 5–10 分钟 AI 剧本写作无法稳定在 10 分钟内完成 | 全剧骨架后只以单路顺序写 5 镜小段，单个异常段会阻断全剧，缺少本地生产合同保底 | 0.13.31 | 5/10 分钟全剧本地编译、H3 双说话人、商品后置及参考片硬审计回归 | 已验证 |
| BUG-019 | 8 分钟简短模式写作慢、蓝图全开会与生成提示互相冲突，异常时仍可能停在失败页 | 生成、参考片、蓝图和本地审计存在四套冲突对白指标；选题会二次请求且无本地候选；完整短句曾被按字符截断；海螺说话人分配会把双人攻防的听者误判为静默人物 | 0.13.32 | `eight-minute-simple-sla.test.js`、`direct-fast-script.test.js`、`quality-blueprint-details.test.js`、全量 120 项、打包态与安装态审计 | 已验证 |
| BUG-020 | 最新版看不到多种剧本模式，点击 AI 写剧本也没有弹出选择 | 剧本模式只在写作入口的临时弹窗确认，没有纳入项目制作策略；历史项目还可能沿用未确认状态 | 0.13.33 | `mode-strategy-audit-and-provider-regression.test.js`、打包态与安装态 UI 审计 | 已验证 |
| BUG-021 | 没有设置审核蓝图时仍出现审核拦截或启动瞬间显示开启 | 后端默认配置、历史配置归一化和 HTML 初始勾选值不是同一套显式启用语义 | 0.13.33 | `quality-blueprint-gate.test.js`、`mode-strategy-audit-and-provider-regression.test.js`、打包态与安装态 UI 审计 | 已验证 |
| BUG-022 | 人物视频提示缺少正脸并整条资产链失败；云端算力节点 隔离后其余资产和分镜也被阻断 | 人物视频依赖没有按本地像塑/云端 H3 模式分层；独立人物图缺失时直接终止；远端暂停被误记为整批失败 | 0.13.33 | `mode-strategy-audit-and-provider-regression.test.js`、视频供应商矩阵、20 秒最短链路矩阵 | 已验证 |
| BUG-023 | 官网与余额正常，但桌面偶发“文本模型连接失败” | Electron 在收到 HTTP 响应前会偶发抛出 `net::ERR_FAILED`，旧恢复器只识别 `ERR_EMPTY_RESPONSE`，没有按同一幂等号切换网络栈恢复 | 0.13.33 | `text-recovery-regression.test.js`、生产余额双网络栈探针、生产文本最小探针 | 已验证 |
| BUG-024 | 前端感叹号过多、右侧队列过长、无后端调用时仍显示运行 | 帮助信息依附图标；队列统计与历史未分层；界面直接信任持久化的 `automation.status`，没有核对进程内操作与真实视频任务 | 0.13.34 | `ui-integration-regression.test.js`、`startup-performance-regression.test.js`、按钮与窗口缩放打包态审计 | 待安装包审计 |
| BUG-025 | 音色库无法选中音色，新项目看不到其他项目的完整资产 | 人物音色仍走旧侧栏绑定；可复用库只收人物和场景的一部分候选，商品、服装、道具、分镜、视频与音频没有统一全局索引 | 0.13.34 | `manual-entry-fast-script.test.js` 跨项目人物/场景/音色绑定、九类独立资产打包态审计 | 待安装包审计 |
| BUG-026 | 系统提示词“查看示例”与实际模块对不上，上传或生成后不知道下一步点哪里 | 提示词说明按名称猜测模块且缺少键级输入输出契约；流程没有依据项目状态计算唯一下一步 | 0.13.34 | `ui-integration-regression.test.js`、65 项提示词实机键级审计、全流程下一步高亮审计 | 待安装包审计 |
| BUG-027 | 只有 `A：xxx / B：xxx` 的对白稿无法轻改并生成完整剧本 | 原上传入口只支持原文保留或完整 AI 写作，没有“人物代号重构 + 逐句轻改 + 故事不可变”独立合同和失败回退 | 0.13.34 | `dialogue-rewrite-contract.test.js`、IPC/界面入口与本地保真回退审计 | 待安装包审计 |
| BUG-028 | 蓝图审核显示 0/13，资产阶段仍以“生产硬合同未通过”拦截用户 | 资产、视频和合成入口只检查了审核总开关，没有同时检查剧本模块与“制作结构”明细；关闭明细后仍误入硬合同 | 0.13.35 | `blueprint-ai-repair-regression.test.js` 三层开关组合、0/13 非阻断、自动修复与一键改错链路、打包态蓝图审计 | 待安装包审计 |
| BUG-029 | 蓝图面板、阶段状态条和提示消息遮挡页面；审核失败只能停下不能自行改错 | 固定弹层未为右侧检查器让位，阶段状态使用粘滞定位；蓝图失败没有“失败镜头范围→AI 定点修复→本地复检”的闭环 | 0.13.35 | `blueprint-ai-repair-regression.test.js`、1718×995/多缩放窗口矩阵、打包态一键改错入口审计 | 待安装包审计 |

| BUG-030 | 开启审核后只能拦截，无法人工确认继续使用原资产 | 质检失败态与生产失败态混用，缺少修复与人工放行双路径 | 0.13.36 | `quality-advisory-regression.test.js`、修复/忽略 IPC 与候选资产强制确认回归 | 已验证 |
| BUG-031 | 无人场景提示词和图片中没有人物，仍被场景质检误判并拦截 | 整图感知哈希相似度被错误当成人物证据，且审核输入混入否定词和剧情人物信息 | 0.13.36 | `scene-empty-qc-regression.test.js`、空镜提示词隔离与明确人脸证据回归 | 已验证 |
| BUG-032 | 用户上传剧本报 `D001 必须绑定1次，实际2次` | 同一句对白在镜头和子镜头结构副本中被重复计数，没有按对白账本做唯一归属 | 0.13.36 | `scene-empty-qc-regression.test.js` 上传对白结构去重回归 | 已验证 |

| BUG-033 | 分镜视频抽卡直接显示 `PROMPT_TOO_LONG` | 系统把对白、表演、连续性、参考素材和重复全局约束整段拼接，清波通道 B 提交层超过 2000 字符即失败，提交前没有保真压缩 | 0.13.37 | `video-prompt-length-regression.test.js`、八模式矩阵和对白完整性回归 | 已验证 |

| BUG-034 | 分镜引用不存在人物、缺少上游分镜时流程直接停住 | 历史分镜人物 ID 未做迁移，后续阶段只断言依赖而不自动回补 | 0.13.38 | 人物引用、缺项回补与断点续跑回归 | 待打包验证 |
| BUG-035 | 系统提示词仍可能在其他视频路径触发 `PROMPT_TOO_LONG` | 旧修复只覆盖单一编译分支，未在统一提交层限制执行副本 | 0.13.38 | H3/Seedance/手动提示统一提交长度回归 | 待打包验证 |
| BUG-036 | 上传剧本被拆出多个“用户原稿场景N” | 本地拆镜兜底把文本分块编号误当作真实场景 | 0.13.38 | 上传剧本场景归并回归 | 待打包验证 |
| BUG-037 | 自动修复把旧分镜标记为不可用且禁止重新选择 | 制作修订与资产删除概念混用，历史候选缺少恢复到当前版本的路径 | 0.13.38 | 历史候选恢复与原文件保留回归 | 待打包验证 |
| BUG-038 | 用户上传人物图提示无法解码 | 上传入口只调用 Electron 单一解码器，缺少常见图片格式、错误后缀与色彩配置的 FFmpeg 自动转码兜底 | 0.13.39 | 用户实报 PNG + 常见图片格式自动转 PNG 回归 | 待打包验证 |
| BUG-039 | 所有阶段“恢复为当前版本”没有反应，分镜合图无法选中 | 历史卡曾选中过就被错误禁用；恢复生成新候选后仍用旧候选 ID 做选中判断 | 0.13.39 | `upload-restore-hotfix-regression.test.js` | 待打包验证 |
| BUG-040 | 人物视频确认后没有立即生成音色资产 | 自动提取只挂在 AI 生成人物视频路径，手动上传和历史恢复未进入同一后处理链 | 0.13.39 | `upload-restore-hotfix-regression.test.js` | 待打包验证 |
| BUG-041 | 历史任务队列仍显示上游基础设施名称 | 只清理了新源码文案，历史任务 message 未在动态渲染时统一脱敏 | 0.13.39 | 动态 DOM 与历史任务文案脱敏回归 | 待打包验证 |
| BUG-042 | 云端视频提示 `prompt contains Chinese outside <d>[Chinese] dialogue blocks` 并中止 | 旧/上传项目的中文阶段名、孤儿人物 ID 或自定义元数据泄漏到 H3 英文画面块；提交前校验只报错，没有确定性自动净化 | 0.13.40 | `hailuo-prompt-contract.test.js` 中文元数据自动修复与原对白逐字保留回归 | 待打包验证 |
| BUG-043 | 历史分镜合图显示暗色且“恢复为当前版本”没有可见反馈 | 历史卡仍被 0.66 透明度伪装成禁用态，并复用普通确认 IPC，刷新后没有独立恢复反馈 | 0.13.41 | 历史候选独立恢复 IPC、当前修订克隆选中、旧文件保留与非暗色 UI 回归 | 已验证 |
| BUG-044 | 已手动绑定音色后，继续任务仍生成人物视频 | 资产计划只用人物视频判断 `character_video` 完成，没有把已落成的项目/全局库音色视为满足声音依赖 | 0.13.41 | 已绑定音色跳过人物视频计划与运行前二次兜底回归 | 已验证 |
| BUG-045 | 上一步完成后点“继续任务”只加载数秒便停止 | 右侧继续按钮按旧 automation.stage 恢复，未按当前修订的真实资产/分镜/视频缺口计算下一环节 | 0.13.41 | 项目总控实时 `nextStage` 驱动继续任务回归 | 已验证 |
| BUG-046 | 点击历史分镜后主卡仍显示待生成，按钮文案暴露“恢复版本” | 历史候选沿用旧依赖指纹，人工选中后仍可能被自动 stale 标记隐藏；跨项目入库还存在未受总开关约束的旧质检判断 | 0.13.42 | 人工选中最高优先级、当前依赖快照重建、蓝图全关全链路旁路与“选中此镜”回归 | 已验证 |
| BUG-047 | 审核蓝图关闭后，部分新生成或断点恢复资产仍暗中调用质检 | 外层流程只处理“质检返回跳过”，没有保证审计函数本身调用次数为零 | 0.13.43 | `blueprint-off-global-bypass.test.js` 新生成、历史恢复、人物、场景、分镜图和视频零调用故障注入 | 已验证 |
| BUG-048 | 开启审核后，系统自己生成的剧本或视频质检未过会停止全流程并显示失败 | 创作质量评分、生产技术错误和用户输入错误共用失败状态，缺少“仅提醒、可修复、可忽略”的统一语义 | 0.13.43 | `quality-advisory-regression.test.js`、`adaptive-agent-adversarial-regression.test.js`、全量回归 | 已验证 |
| BUG-049 | 一次暂停写作扩散为几十个 `SCRIPT_GENERATION_PAUSED` / `SHOT_QUALITY_RETRY_EXHAUSTED` 失败 | 并发 worker 各自把同一个控制信号记录成镜头失败 | 0.13.43 | 36 镜暂停故障注入，验证只传播一个控制错误 | 已验证 |
| BUG-050 | 分镜合图被供应商强制成 9:16，继而错误提示 4×2/9:8 尺寸不合格 | 逻辑画布比例直接传给只支持固定比例的图片接口，接口静默改写尺寸 | 0.13.43 | 动态分镜合图逻辑比例与供应商画布映射回归 | 已验证 |
| BUG-051 | 新增带货镜头数后，部分写作或上传剧本解析报 `Cannot add property commerceShotCount, object is not extensible` | 向冻结的时长规划对象原地写入新字段 | 0.13.43 | 8 分钟简易剧本、自然上传剧本、失败分段本地补齐和 30 分钟解析回归 | 已验证 |
| BUG-052 | 一键生成选题反复得到同一批 10 个题材 | 成功状态仍复用上一批 `requestSessionId`，服务端幂等缓存返回旧结果；本地兜底也只有固定一批 | 0.13.44 | 成功后新会话、上一批禁用集、两套本地轮换批次回归 | 已验证 |
| BUG-053 | 上传剧本的“人物介绍/故事简介”被做成成片镜头 | 人物元数据与正式剧情共用同一时间线分块，没有输入协议边界 | 0.13.44 | 上传稿元数据/正式剧情物理分流与逐句账本回归 | 已验证 |
| BUG-054 | 成片出现字幕、标题或人物介绍文字 | 拼接器把旧 `criticalOnScreenText` 确定性转成 FFmpeg `drawtext` 烧录，直接违反无字成片合同 | 0.13.44 | 两条拼接分支均为零 `drawtext` 回归 | 已验证 |
| BUG-055 | 人物四视图背景出现烟雾、渐变、黑边和不同色块 | 旧提示仅写“中性浅灰”，没有固定颜色、全画布一致和无黑边硬合同 | 0.13.44 | `#D9D9D9` 固定背景合同及黑边/烟雾负向回归 | 已验证 |
| BUG-056 | 安装位置与项目素材保存位置不可选 | NSIS 使用一键安装；工作台根目录只从 Electron 默认 userData 派生，没有安全迁移入口 | 0.13.44 | assisted installer、自定义保存根、只复制不删除与重启恢复回归 | 已验证 |
| BUG-057 | 客户更新后人物和分镜图片统一裂图 | 渲染层长期保存并复用 `file:///` 地址；Electron 安全上下文升级、中文路径和 URL 保留字符会使地址失效，旧验收只检查 DOM 未检查像素解码 | 0.13.45 | 受控 `puream-asset://` 协议、根目录边界、中文/特殊字符、打包版与安装版 `naturalWidth > 0` 回归 | 已验证 |
| BUG-058 | 上传或选择新人物三视图后仍显示并使用旧合板 | 三类人物身份资产各自维护 selected，界面与工作流又使用不同的固定优先级，没有唯一身份指针 | 0.13.45 | `activeIdentityCandidateId` 跨合板/三视图/介绍图唯一选择与下游一致性回归 | 已验证 |
| BUG-059 | 非系统模板剧本识别不稳定，人物介绍可能混入剧情 | 上传入口缺少确定性格式探测，只稳定支持同行“角色：对白”和自有模板 | 0.13.45 | 六类格式探测、独占行角色、时间码、BOM/CRLF、正文分界及对白原文顺序回归 | 已验证 |
| BUG-060 | 一键做剧与分步做剧在断点状态下可能给用户不同的下一步感知 | UI 状态提示曾优先使用历史 automation.stage，而真实缺失项由另一套统计推断 | 0.13.45 | 统一以真实素材完成度决定继续阶段；分步不跨阶段、显式一键允许跨阶段、重复点击互斥和失败断点回归 | 已验证 |
| BUG-061 | `【场景】` 剧本被识别成普通对白稿 | 格式探测只识别“场景：”和“第N场”，没有把中文方头括号场景标题列为影视剧本语法 | 0.13.46 | `script-scene-ledger-regression.test.js` 入口优先级与格式矩阵 | 已验证 |
| BUG-062 | `A -> B` 或 `A / B` 两个场景只生成一个场景资产 | 场景标题被当作不可拆分字符串，后续精确名称归并又把复合标题永久固化 | 0.13.46 | 复合标题顺序拆分、内联转场、六场景七次出现回归 | 已验证 |
| BUG-063 | 长剧本分块后凭空出现“剧情主要空间” | 文本分块不携带进入分块前已经生效的场景上下文，本地兜底只能创建占位场景 | 0.13.46 | 跨分块活动场景继承、无占位场景、上游失败本地兜底回归 | 已验证 |
| BUG-064 | 模型返回合并场景或错误场景名后直接进入资产与分镜 | 原稿场景只存在于提示词，没有分析后的确定性来源绑定与完整性断言 | 0.13.46 | 恶意模型输出修正、逐句对白来源场景绑定、缺场失败关闭回归 | 已验证 |
| BUG-065 | 修复一个入口后同类问题在另一入口反复出现 | 项目状态、提示拼装、质量判断和资产选择散落在多处，JSON 既是状态又是镜像，没有统一合同和权威提交点 | 0.14.0 | 制作合同、SQLite 当前态、修订快照、审计事件、一键/分步同核及 197 项全量回归 | 已验证 |
| BUG-066 | 修改素材保存位置后，新目录中的图片仍可能全部裂图 | 旧实现只复制目录，没有重写项目和素材库中的绝对路径、`file://` 及安全素材 URL | 0.14.0 | 保存位置事务迁移、数据库/JSON/三类地址换根、旧目录保留及安全根边界回归 | 已验证 |
| BUG-067 | 不同格式剧本或附件说明会干扰剧情、场景和输出禁令 | 原文、元数据、模型建议和机器硬规则没有显式权威层级 | 0.14.0 | 本地预编译、来源权威、越权说明抑制、场景/对白账本和机器绝对禁令回归 | 已验证 |
| BUG-068 | 本地兜底虽然能继续流程，但可能把低质量结构直接送入付费生成 | “可恢复”与“可正式出片”共用一个成功状态，没有分级准入门 | 0.14.0 | L1/L2/L3 质量报告、正式付费前门禁、低质量兜底仅诊断回归 | 已验证 |
| BUG-069 | 多类人物资产同时存在时，界面当前图与后续使用图可能不一致 | 候选资产缺少统一来源护照和唯一当前绑定 | 0.14.0 | 资产护照、文件/依赖指纹、唯一当前身份和介绍类资产禁入成片回归 | 已验证 |
| BUG-070 | 多次一键写选题在更多轮次后仍可能回到固定池 | 旧方案只更换请求会话和少量静态备选，没有把历史标题与批次指纹作为持久多样性约束 | 0.14.0 | 历史禁用集、批次指纹、确定性多池轮换及连续三批零重复回归 | 已验证 |
| BUG-071 | 更新后打开人物形象库长时间卡住，看起来像图片无法显示 | 旧实现每次启动都遍历全部项目；对每个候选又重读项目、哈希和复制文件并重写完整索引，界面一次拉取全部媒体并预读视频元数据，音色 WAV 还被错误挂到隐藏图片标签 | 0.14.0 | 权威持久索引、每项目单次读取、批量一次写入、按类型 IPC、图片懒解码、视频零预载、音频专用占位及真实安装态 5 秒打开门槛 | 已验证 |
| BUG-072 | 海螺分镜视频大量报 `HAILUO_PROMPT_DIALOGUE_PERFORMANCE_MISSING`，同批只有少数镜头可生成 | 海螺专用编译器已经在 1900 字符内保留完整对白，但统一提交层又追加一套输出禁令并从尾部盲截，导致对白表演合同或禁令被截断；两层单测均通过，却没有组合链路测试 | 0.14.2 | `hailuo-final-submission-integrity.test.js`、`hailuo-prompt-contract.test.js`、`video-prompt-length-regression.test.js`、客户真实 30 分镜最终提交对象逐条审计 | 已验证 |
| BUG-073 | 对话换人后画面仍停在上一名说话人，人物嘴型和视线对象错位 | 单个供应商视频任务同时承担多名说话人、多个机位和内部切镜；分镜合图又把整张时间轴沿用父镜头第一名主体，提示词无法可靠覆盖模型的单一视觉注意力 | 0.15.0 | `agent-director-contract.test.js`、`agent-director-ffmpeg.test.js`、`prompt-matrix-dialogue-regression.test.js` | 已验证 |
| BUG-074 | 创作链路的局部失败可能触发固定模板补位，或旧兼容入口绕开全局 Agent | 创意生成、供应商提交和查询仍存在少量分散旁路，失败时“继续流程”优先级高于“保持用户剧情与生产合同” | 0.15.0 | `adaptive-agent-adversarial-regression.test.js`、`dialogue-rewrite-contract.test.js`、`uploaded-analysis-resume-regression.test.js`、`root-contract-regression.test.js` | 已验证 |
| BUG-075 | 为修复换人不切镜而把每次说话人或机位变化都拆成独立视频任务，导致连续对白节奏割裂、表演不连贯且调用数增加 | v0.15.0 混淆了对白事实、编辑镜头段与供应商任务三层边界；快速剧本编译器还会把多轮对白扁平化到开场人物，旧提示词入口残留相反规则 | 0.15.1 | 连续生成块 v4、付费前原子降级预编译、成片切镜证据审计、`agent-director-contract.test.js`、`agent-director-ffmpeg.test.js`、`direct-fast-script.test.js`、全 H3 入口陈旧规则扫描及打包/安装/真实升级验收 | 已验证 |

## 维护规则

- 每个修复都递增最小单元版本号。
- 不删除历史条目；复发时在原编号追加复发版本和新根因。
- 官网钱包是余额、冻结金额、充值订单与到账状态的唯一权威，桌面端不维护独立余额。
- “已验证”必须同时具备源码回归用例和打包后检查；涉及官网的条目还必须保留线上或候选接口验证结果。
