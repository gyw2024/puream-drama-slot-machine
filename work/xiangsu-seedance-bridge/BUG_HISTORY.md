# 纯梦短剧老虎机 Bug 台账

本台账记录已确认的用户问题、根因、首次修复版本和必须持续执行的回归检查。状态只有通过源码测试与安装包审计后才能标记为“已验证”。

| 编号 | 用户现象 | 根因 | 首次修复版本 | 回归检查 | 当前状态 |
| --- | --- | --- | --- | --- | --- |
| BUG-001 | 一键生成选题被中止或 JSON 结构不匹配 | 中转返回包裹层与客户端严格结构解析不兼容 | 0.13.28 | `topic-relay-regression.test.js` | 已验证 |
| BUG-002 | 上传长剧本后解析、拆镜在 5 分钟停止 | 客户端存在固定文本期限且上游内容未分块 | 0.13.28 | `script-format-no-deadline-regression.test.js`、`uploaded-analysis-resume-regression.test.js` | 已验证 |
| BUG-003 | 审核蓝图关闭后仍编译、打回或拦截 | 审核开关只影响界面，未贯穿编译与返修入口 | 0.13.28 | `blueprint-off-global-bypass.test.js`、`blueprint-off-prompt-compiler-regression.test.js` | 已验证 |
| BUG-004 | 逐秒合图仍被尾帧或公网参考图拦截 | 模式矩阵复用了首尾帧连续性合同 | 0.13.27 | `twenty-second-full-matrix.test.js` | 已验证 |
| BUG-005 | 上一阶段结束后视频自动生成，按钮仍显示不可用 | 任务恢复与用户触发状态混用 | 0.13.27 | `adversarial-state.test.js` | 已验证 |
| BUG-006 | 手改分镜提示词后“再抽一次”没有运行 | 编辑态被蓝图校验与未提交弹层拦截 | 0.13.28 | `blueprint-off-global-bypass.test.js` | 已验证 |
| BUG-007 | 新建项目不能并行、切换项目卡死、历史项目无法删除 | 全局 busy 状态代替了项目级任务状态，删除缺少回收区 | 0.13.28 | `adversarial-state.test.js` | 已验证 |
| BUG-008 | 用户上传剧本却仍按设置时长强行改写 | 上传模式与 AI 全生成共用时长合同 | 0.13.28 | `duration-contract-regression.test.js` | 已验证 |
| BUG-009 | 人物资产库新项目为空 | 可复用资产被错误限定在当前项目 | 0.13.28 | `adversarial-state.test.js` | 已验证 |
| BUG-010 | 视频提示词缺少说话人、语气、听者和完整对白 | 提示词编译未把对白账本设为最高优先级 | 0.13.28 | `hailuo-prompt-contract.test.js`、`prompt-matrix-dialogue-regression.test.js` | 已验证 |
| BUG-011 | 长时间写剧本但文本扣费显示为 0 | SSE 结算回执没有同步到项目文案账本 | 0.13.28 | `timed-storyboard-prompt-cost-regression.test.js` | 已验证 |
| BUG-012 | 文本阶段显示上游原始 JSON/额度错误 | 服务端原始异常未经公开错误映射 | 0.13.28 | `topic-relay-regression.test.js` | 已验证 |
| BUG-013 | 充值后写剧本仍在约 5 分钟处显示连接失败 | 同项目同时发出 3 个大文本流；网络中断被标记为禁止重试且整阶段失败 | 0.13.29 | `text-recovery-regression.test.js`、`manual-entry-fast-script.test.js`、安装包审计、官网 r79 文本接口公网探针 | 已验证 |
| BUG-014 | 软件内无法查看官网余额或直接充值 | 桌面端未接入官网统一钱包与微信充值订单 | 0.13.29 | `wallet-recharge-contract.test.js`、官网钱包专项 4/4、官网 r79 余额接口公网探针、安装包审计 | 已验证 |
| BUG-015 | 已准备三种剧本示例，但历史项目和上传剧本用户看不到 | 示例入口只存在于首次 AI 写作格式弹窗，弹窗确认后不再出现 | 0.13.30 | `script-format-no-deadline-regression.test.js`、打包态与安装态审计 | 已验证 |
| BUG-016 | 软件内充值门槛与运营要求不一致 | 桌面端直接沿用了官网 30 元最低充值规则 | 0.13.30 | `wallet-recharge-contract.test.js`、充值弹窗打包态与安装态审计 | 已验证 |
| BUG-017 | 写文案运行一小时后提示“应拆成 4 个生成单元，实际返回 0 个” | 文本请求没有截止时间；上传剧本拆镜并发超过官网两路上限；解析未兼容常见包裹层；上游返回说明文字时客户端重复请求后仍整阶段失败 | 0.13.31 | `direct-fast-script.test.js`、`uploaded-analysis-resume-regression.test.js`、`uploaded-script-local-fallback.test.js`、全量测试、打包态与安装态审计 | 已验证 |
| BUG-018 | 5–10 分钟 AI 剧本写作无法稳定在 10 分钟内完成 | 全剧骨架后只以单路顺序写 5 镜小段，单个异常段会阻断全剧，缺少本地生产合同保底 | 0.13.31 | 5/10 分钟全剧本地编译、H3 双说话人、商品后置及参考片硬审计回归 | 已验证 |
| BUG-019 | 8 分钟简短模式写作慢、蓝图全开会与生成提示互相冲突，异常时仍可能停在失败页 | 生成、参考片、蓝图和本地审计存在四套冲突对白指标；选题会二次请求且无本地候选；完整短句曾被按字符截断；海螺说话人分配会把双人攻防的听者误判为静默人物 | 0.13.32 | `eight-minute-simple-sla.test.js`、`direct-fast-script.test.js`、`quality-blueprint-details.test.js`、全量 120 项、打包态与安装态审计 | 已验证 |
| BUG-020 | 最新版看不到多种剧本模式，点击 AI 写剧本也没有弹出选择 | 剧本模式只在写作入口的临时弹窗确认，没有纳入项目制作策略；历史项目还可能沿用未确认状态 | 0.13.33 | `mode-strategy-audit-and-provider-regression.test.js`、打包态与安装态 UI 审计 | 已验证 |
| BUG-021 | 没有设置审核蓝图时仍出现审核拦截或启动瞬间显示开启 | 后端默认配置、历史配置归一化和 HTML 初始勾选值不是同一套显式启用语义 | 0.13.33 | `quality-blueprint-gate.test.js`、`mode-strategy-audit-and-provider-regression.test.js`、打包态与安装态 UI 审计 | 已验证 |
| BUG-022 | 人物视频提示缺少正脸并整条资产链失败；云端算力节点 隔离后其余资产和分镜也被阻断 | 人物视频依赖没有按本地像塑/云端 H3 模式分层；独立人物图缺失时直接终止；远端暂停被误记为整批失败 | 0.13.33 | `mode-strategy-audit-and-provider-regression.test.js`、视频供应商矩阵、20 秒最短链路矩阵 | 已验证 |
| BUG-023 | 官网与余额正常，但桌面偶发“文本模型连接失败” | Electron 在收到 HTTP 响应前会偶发抛出 `net::ERR_FAILED`，旧恢复器只识别 `ERR_EMPTY_RESPONSE`，没有按同一幂等号切换网络栈恢复 | 0.13.33 | `text-recovery-regression.test.js`、生产余额双网络栈探针、生产文本最小探针 | 已验证 |
| BUG-024 | 前端感叹号过多、右侧队列过长、无后端调用时仍显示运行 | 帮助信息依附图标；队列统计与历史未分层；界面直接信任持久化的 `automation.status`，没有核对进程内操作与真实视频任务 | 0.13.34 | `ui-integration-regression.test.js`、`startup-performance-regression.test.js`、按钮与窗口缩放打包态审计 | 待安装包审计 |
| BUG-025 | 音色库无法选中音色，新项目看不到其他项目的完整资产 | 人物音色仍走旧侧栏绑定；可复用库只收人物和场景的一部分候选，商品、服装、道具、分镜、视频与音频没有统一全局索引 | 0.13.34 | `manual-entry-fast-script.test.js` 跨项目人物/场景/音色绑定、九类独立资产打包态审计 | 待安装包审计 |
| BUG-026 | 系统提示词“查看示例”与实际模块对不上，上传或生成后不知道下一步点哪里 | 提示词说明按名称猜测模块且缺少键级输入输出契约；流程没有依据项目状态计算唯一下一步 | 0.13.34 | `ui-integration-regression.test.js`、65 项提示词实机键级审计、全流程下一步高亮审计 | 待安装包审计 |
| BUG-027 | 只有 `A：xxx / B：xxx` 的对白稿无法轻改并生成完整剧本 | 原上传入口只支持原文保留或完整 AI 写作，没有“人物代号重构 + 逐句轻改 + 故事不可变”独立合同和失败回退 | 0.13.34 | `dialogue-rewrite-contract.test.js`、IPC/界面入口与本地保真回退审计 | 待安装包审计 |
| BUG-028 | 蓝图审核显示 0/13，资产阶段仍以“生产硬合同未通过”拦截用户 | 资产、视频和合成入口只检查了审核总开关，没有同时检查剧本模块与“制作结构”明细；关闭明细后仍误入硬合同 | 0.13.35 | `blueprint-ai-repair-regression.test.js` 三层开关组合、0/13 非阻断、自动修复与一键改错链路、打包态蓝图审计 | 待安装包审计 |
| BUG-029 | 蓝图面板、阶段状态条和提示消息遮挡页面；审核失败只能停下不能自行改错 | 固定弹层未为右侧检查器让位，阶段状态使用粘滞定位；蓝图失败没有“失败镜头范围→AI 定点修复→本地复检”的闭环 | 0.13.35 | `blueprint-ai-repair-regression.test.js`、1718×995/多缩放窗口矩阵、打包态一键改错入口审计 | 待安装包审计 |

| BUG-030 | 开启审核后只能拦截，无法人工确认继续使用原资产 | 质检失败态与生产失败态混用，缺少修复与人工放行双路径 | 0.13.36 | `quality-advisory-regression.test.js`、修复/忽略 IPC 与候选资产强制确认回归 | 已验证 |
| BUG-031 | 无人场景提示词和图片中没有人物，仍被场景质检误判并拦截 | 整图感知哈希相似度被错误当成人物证据，且审核输入混入否定词和剧情人物信息 | 0.13.36 | `scene-empty-qc-regression.test.js`、空镜提示词隔离与明确人脸证据回归 | 已验证 |
| BUG-032 | 用户上传剧本报 `D001 必须绑定1次，实际2次` | 同一句对白在镜头和子镜头结构副本中被重复计数，没有按对白账本做唯一归属 | 0.13.36 | `scene-empty-qc-regression.test.js` 上传对白结构去重回归 | 已验证 |

| BUG-033 | 分镜视频抽卡直接显示 `PROMPT_TOO_LONG` | 系统把对白、表演、连续性、参考素材和重复全局约束整段拼接，清波通道 B 提交层超过 2000 字符即失败，提交前没有保真压缩 | 0.13.37 | `video-prompt-length-regression.test.js`、八模式矩阵和对白完整性回归 | 已验证 |

| BUG-034 | 分镜引用不存在人物、缺少上游分镜时流程直接停住 | 历史分镜人物 ID 未做迁移，后续阶段只断言依赖而不自动回补 | 0.13.38 | 人物引用、缺项回补与断点续跑回归 | 待打包验证 |
| BUG-035 | 系统提示词仍可能在其他视频路径触发 `PROMPT_TOO_LONG` | 旧修复只覆盖单一编译分支，未在统一提交层限制执行副本 | 0.13.38 | H3/Seedance/手动提示统一提交长度回归 | 待打包验证 |
| BUG-036 | 上传剧本被拆出多个“用户原稿场景N” | 本地拆镜兜底把文本分块编号误当作真实场景 | 0.13.38 | 上传剧本场景归并回归 | 待打包验证 |
| BUG-037 | 自动修复把旧分镜标记为不可用且禁止重新选择 | 制作修订与资产删除概念混用，历史候选缺少恢复到当前版本的路径 | 0.13.38 | 历史候选恢复与原文件保留回归 | 待打包验证 |
| BUG-038 | 用户上传人物图提示无法解码 | 上传入口只调用 Electron 单一解码器，缺少常见图片格式、错误后缀与色彩配置的 FFmpeg 自动转码兜底 | 0.13.39 | 用户实报 PNG + 常见图片格式自动转 PNG 回归 | 待打包验证 |
| BUG-039 | 所有阶段“恢复为当前版本”没有反应，分镜合图无法选中 | 历史卡曾选中过就被错误禁用；恢复生成新候选后仍用旧候选 ID 做选中判断 | 0.13.39 | `upload-restore-hotfix-regression.test.js` | 待打包验证 |
| BUG-040 | 人物视频确认后没有立即生成音色资产 | 自动提取只挂在 AI 生成人物视频路径，手动上传和历史恢复未进入同一后处理链 | 0.13.39 | `upload-restore-hotfix-regression.test.js` | 待打包验证 |
| BUG-041 | 历史任务队列仍显示上游基础设施名称 | 只清理了新源码文案，历史任务 message 未在动态渲染时统一脱敏 | 0.13.39 | 动态 DOM 与历史任务文案脱敏回归 | 待打包验证 |
| BUG-042 | 云端视频提示 `prompt contains Chinese outside <d>[Chinese] dialogue blocks` 并中止 | 旧/上传项目的中文阶段名、孤儿人物 ID 或自定义元数据泄漏到 H3 英文画面块；提交前校验只报错，没有确定性自动净化 | 0.13.40 | `hailuo-prompt-contract.test.js` 中文元数据自动修复与原对白逐字保留回归 | 待打包验证 |
| BUG-043 | 历史分镜合图显示暗色且“恢复为当前版本”没有可见反馈 | 历史卡仍被 0.66 透明度伪装成禁用态，并复用普通确认 IPC，刷新后没有独立恢复反馈 | 0.13.41 | 历史候选独立恢复 IPC、当前修订克隆选中、旧文件保留与非暗色 UI 回归 | 已验证 |
| BUG-044 | 已手动绑定音色后，继续任务仍生成人物视频 | 资产计划只用人物视频判断 `character_video` 完成，没有把已落成的项目/全局库音色视为满足声音依赖 | 0.13.41 | 已绑定音色跳过人物视频计划与运行前二次兜底回归 | 已验证 |
| BUG-045 | 上一步完成后点“继续任务”只加载数秒便停止 | 右侧继续按钮按旧 automation.stage 恢复，未按当前修订的真实资产/分镜/视频缺口计算下一环节 | 0.13.41 | 项目总控实时 `nextStage` 驱动继续任务回归 | 已验证 |
| BUG-046 | 点击历史分镜后主卡仍显示待生成，按钮文案暴露“恢复版本” | 历史候选沿用旧依赖指纹，人工选中后仍可能被自动 stale 标记隐藏；跨项目入库还存在未受总开关约束的旧质检判断 | 0.13.42 | 人工选中最高优先级、当前依赖快照重建、蓝图全关全链路旁路与“选中此镜”回归 | 已验证 |
| BUG-047 | 审核蓝图关闭后，部分新生成或断点恢复资产仍暗中调用质检 | 外层流程只处理“质检返回跳过”，没有保证审计函数本身调用次数为零 | 0.13.43 | `blueprint-off-global-bypass.test.js` 新生成、历史恢复、人物、场景、分镜图和视频零调用故障注入 | 已验证 |
| BUG-048 | 开启审核后，系统自己生成的剧本或视频质检未过会停止全流程并显示失败 | 创作质量评分、生产技术错误和用户输入错误共用失败状态，缺少“仅提醒、可修复、可忽略”的统一语义 | 0.13.43 | `quality-advisory-regression.test.js`、`adaptive-agent-adversarial-regression.test.js`、全量回归 | 已验证 |
| BUG-049 | 一次暂停写作扩散为几十个 `SCRIPT_GENERATION_PAUSED` / `SHOT_QUALITY_RETRY_EXHAUSTED` 失败 | 并发 worker 各自把同一个控制信号记录成镜头失败 | 0.13.43 | 36 镜暂停故障注入，验证只传播一个控制错误 | 已验证 |
| BUG-050 | 分镜合图被供应商强制成 9:16，继而错误提示 4×2/9:8 尺寸不合格 | 逻辑画布比例直接传给只支持固定比例的图片接口，接口静默改写尺寸 | 0.13.43 | 动态分镜合图逻辑比例与供应商画布映射回归 | 已验证 |
| BUG-051 | 新增带货镜头数后，部分写作或上传剧本解析报 `Cannot add property commerceShotCount, object is not extensible` | 向冻结的时长规划对象原地写入新字段 | 0.13.43 | 8 分钟简易剧本、自然上传剧本、失败分段本地补齐和 30 分钟解析回归 | 已验证 |
| BUG-052 | 一键生成选题反复得到同一批 10 个题材 | 成功状态仍复用上一批 `requestSessionId`，服务端幂等缓存返回旧结果；本地兜底也只有固定一批 | 0.13.44 | 成功后新会话、上一批禁用集、两套本地轮换批次回归 | 已验证 |
| BUG-053 | 上传剧本的“人物介绍/故事简介”被做成成片镜头 | 人物元数据与正式剧情共用同一时间线分块，没有输入协议边界 | 0.13.44 | 上传稿元数据/正式剧情物理分流与逐句账本回归 | 已验证 |
| BUG-054 | 成片出现字幕、标题或人物介绍文字 | 拼接器把旧 `criticalOnScreenText` 确定性转成 FFmpeg `drawtext` 烧录，直接违反无字成片合同 | 0.13.44 | 两条拼接分支均为零 `drawtext` 回归 | 已验证 |
| BUG-055 | 人物四视图背景出现烟雾、渐变、黑边和不同色块 | 旧提示仅写“中性浅灰”，没有固定颜色、全画布一致和无黑边硬合同 | 0.13.44 | `#D9D9D9` 固定背景合同及黑边/烟雾负向回归 | 已验证 |
| BUG-056 | 安装位置与项目素材保存位置不可选 | NSIS 使用一键安装；工作台根目录只从 Electron 默认 userData 派生，没有安全迁移入口 | 0.13.44 | assisted installer、自定义保存根、只复制不删除与重启恢复回归 | 已验证 |
| BUG-057 | 客户更新后人物和分镜图片统一裂图 | 渲染层长期保存并复用 `file:///` 地址；Electron 安全上下文升级、中文路径和 URL 保留字符会使地址失效，旧验收只检查 DOM 未检查像素解码 | 0.13.45 | 受控 `puream-asset://` 协议、根目录边界、中文/特殊字符、打包版与安装版 `naturalWidth > 0` 回归 | 已验证 |
| BUG-058 | 上传或选择新人物三视图后仍显示并使用旧合板 | 三类人物身份资产各自维护 selected，界面与工作流又使用不同的固定优先级，没有唯一身份指针 | 0.13.45 | `activeIdentityCandidateId` 跨合板/三视图/介绍图唯一选择与下游一致性回归 | 已验证 |
| BUG-059 | 非系统模板剧本识别不稳定，人物介绍可能混入剧情 | 上传入口缺少确定性格式探测，只稳定支持同行“角色：对白”和自有模板 | 0.13.45 | 六类格式探测、独占行角色、时间码、BOM/CRLF、正文分界及对白原文顺序回归 | 已验证 |
| BUG-060 | 一键做剧与分步做剧在断点状态下可能给用户不同的下一步感知 | UI 状态提示曾优先使用历史 automation.stage，而真实缺失项由另一套统计推断 | 0.13.45 | 统一以真实素材完成度决定继续阶段；分步不跨阶段、显式一键允许跨阶段、重复点击互斥和失败断点回归 | 已验证 |
| BUG-061 | `【场景】` 剧本被识别成普通对白稿 | 格式探测只识别“场景：”和“第N场”，没有把中文方头括号场景标题列为影视剧本语法 | 0.13.46 | `script-scene-ledger-regression.test.js` 入口优先级与格式矩阵 | 已验证 |
| BUG-062 | `A -> B` 或 `A / B` 两个场景只生成一个场景资产 | 场景标题被当作不可拆分字符串，后续精确名称归并又把复合标题永久固化 | 0.13.46 | 复合标题顺序拆分、内联转场、六场景七次出现回归 | 已验证 |
| BUG-063 | 长剧本分块后凭空出现“剧情主要空间” | 文本分块不携带进入分块前已经生效的场景上下文，本地兜底只能创建占位场景 | 0.13.46 | 跨分块活动场景继承、无占位场景、上游失败本地兜底回归 | 已验证 |
| BUG-064 | 模型返回合并场景或错误场景名后直接进入资产与分镜 | 原稿场景只存在于提示词，没有分析后的确定性来源绑定与完整性断言 | 0.13.46 | 恶意模型输出修正、逐句对白来源场景绑定、缺场失败关闭回归 | 已验证 |
| BUG-065 | 修复一个入口后同类问题在另一入口反复出现 | 项目状态、提示拼装、质量判断和资产选择散落在多处，JSON 既是状态又是镜像，没有统一合同和权威提交点 | 0.14.0 | 制作合同、SQLite 当前态、修订快照、审计事件、一键/分步同核及 197 项全量回归 | 已验证 |
| BUG-066 | 修改素材保存位置后，新目录中的图片仍可能全部裂图 | 旧实现只复制目录，没有重写项目和素材库中的绝对路径、`file://` 及安全素材 URL | 0.14.0 | 保存位置事务迁移、数据库/JSON/三类地址换根、旧目录保留及安全根边界回归 | 已验证 |
| BUG-067 | 不同格式剧本或附件说明会干扰剧情、场景和输出禁令 | 原文、元数据、模型建议和机器硬规则没有显式权威层级 | 0.14.0 | 本地预编译、来源权威、越权说明抑制、场景/对白账本和机器绝对禁令回归 | 已验证 |
| BUG-068 | 本地兜底虽然能继续流程，但可能把低质量结构直接送入付费生成 | “可恢复”与“可正式出片”共用一个成功状态，没有分级准入门 | 0.14.0 | L1/L2/L3 质量报告、正式付费前门禁、低质量兜底仅诊断回归 | 已验证 |
| BUG-069 | 多类人物资产同时存在时，界面当前图与后续使用图可能不一致 | 候选资产缺少统一来源护照和唯一当前绑定 | 0.14.0 | 资产护照、文件/依赖指纹、唯一当前身份和介绍类资产禁入成片回归 | 已验证 |
| BUG-070 | 多次一键写选题在更多轮次后仍可能回到固定池 | 旧方案只更换请求会话和少量静态备选，没有把历史标题与批次指纹作为持久多样性约束 | 0.14.0 | 历史禁用集、批次指纹、确定性多池轮换及连续三批零重复回归 | 已验证 |
| BUG-071 | 更新后打开人物形象库长时间卡住，看起来像图片无法显示 | 旧实现每次启动都遍历全部项目；对每个候选又重读项目、哈希和复制文件并重写完整索引，界面一次拉取全部媒体并预读视频元数据，音色 WAV 还被错误挂到隐藏图片标签 | 0.14.0 | 权威持久索引、每项目单次读取、批量一次写入、按类型 IPC、图片懒解码、视频零预载、音频专用占位及真实安装态 5 秒打开门槛 | 已验证 |
| BUG-072 | 海螺分镜视频大量报 `HAILUO_PROMPT_DIALOGUE_PERFORMANCE_MISSING`，同批只有少数镜头可生成 | 海螺专用编译器已经在 1900 字符内保留完整对白，但统一提交层又追加一套输出禁令并从尾部盲截，导致对白表演合同或禁令被截断；两层单测均通过，却没有组合链路测试 | 0.14.2 | `hailuo-final-submission-integrity.test.js`、`hailuo-prompt-contract.test.js`、`video-prompt-length-regression.test.js`、客户真实 30 分镜最终提交对象逐条审计 | 已验证 |
| BUG-073 | 对话换人后画面仍停在上一名说话人，人物嘴型和视线对象错位 | 单个供应商视频任务同时承担多名说话人、多个机位和内部切镜；分镜合图又把整张时间轴沿用父镜头第一名主体，提示词无法可靠覆盖模型的单一视觉注意力 | 0.15.0 | `agent-director-contract.test.js`、`agent-director-ffmpeg.test.js`、`prompt-matrix-dialogue-regression.test.js` | 已验证 |
| BUG-074 | 创作链路的局部失败可能触发固定模板补位，或旧兼容入口绕开全局 Agent | 创意生成、供应商提交和查询仍存在少量分散旁路，失败时“继续流程”优先级高于“保持用户剧情与生产合同” | 0.15.0 | `adaptive-agent-adversarial-regression.test.js`、`dialogue-rewrite-contract.test.js`、`uploaded-analysis-resume-regression.test.js`、`root-contract-regression.test.js` | 已验证 |
| BUG-075 | 为修复换人不切镜而把每次说话人或机位变化都拆成独立视频任务，导致连续对白节奏割裂、表演不连贯且调用数增加 | v0.15.0 混淆了对白事实、编辑镜头段与供应商任务三层边界；快速剧本编译器还会把多轮对白扁平化到开场人物，旧提示词入口残留相反规则 | 0.15.1 | 连续生成块 v4、付费前原子降级预编译、成片切镜证据审计、`agent-director-contract.test.js`、`agent-director-ffmpeg.test.js`、`direct-fast-script.test.js`、全 H3 入口陈旧规则扫描及打包/安装/真实升级验收 | 已验证 |
| BUG-094 | 资产直投首锚点自阻塞、本地拼接记录误计第二个付费任务、切镜预算和后半段动作不完整 | 前序依赖、上游任务身份、对白说话人变化和动作摘要分别使用了错误结构层；测试遥测还会回落到真实 APPDATA | 0.16.105 | 651/651、3 分钟 27/27 提示词链、唯一真实 provider taskId、打包/简易/安装态审计、测试前后及安装前后用户数据零差异 | 已验证 |
| BUG-095 | 资产直投虽然正确上传人物、场景、物品和音色，H3 仍可能说错人、追加乱码、烧入字幕或忽略后半段动作 | 正式提交路径提前返回自研 `production/references/visual_timeline` 方言，导致下方官方 H3 六段式编译器永远不可达；内部 C/P/W 编号和自然语言音色描述也没有变成官方 Subject/Audio 绑定 | 0.16.106 | 官方六段式引用协议、稳定 S1/S2 与 Audio 绑定、动作/服装/商品引用重写；652/652 回归；真实 S09 单次提交、逐帧与 ASR 验收；总上游成本 ¥14.50 | 已验证 |
| BUG-096 | 资产直投偶发把染发动作做在执行者自己身上、换人后仍由上一人张嘴、个别 H3 成片自行叠加对白文字；五镜完成后的本地拼接还误唤醒文本标准化 | 语义编译只保留动作执行者，没有强制携带承受者及身体部位所有权；说话人约束只在全局出现一次，长提示中容易被稀释；仅省略画面文字概念不足以稳定得到纯摄影画面；拼接错误复用带阶段依赖检查的工作台入口 | 0.16.107 | `hailuo-natural-semantic-regression.test.js` 新增动作受事/逐句镜头口型/纯摄影画面合同；相关 68/68 回归；真实 57.74 秒六次单提交验收，S07 修正版动作、口型与无字画面通过，总实扣 ¥8.25；0.16.107 安装态与打包态审计通过；S06/S09 原成片缺陷保留为复现证据 | 已验证 |
| BUG-097 | 新模式表演感染力不足、群像动作仍只拍主角、人物站位朝向不稳；AI 选题写剧偶发流程继续但剧情人物和场景变成通用占位内容 | 表演提示缺少句内声调/呼吸/表情三阶段变化、画面规划没有把原稿群像动作编译为独立反应镜头、站位与视线没有成为每句对白的绑定字段；全剧骨架 JSON 结构轻微不合格时旧代码直接采用通用本地骨架，且相同自由格式上传在三模式重复标准化；Electron 文本读取连接消失后还可能永久等待 | 0.16.107 | `h3-emotion-blocking-ensemble-regression.test.js`、`direct-fast-script.test.js`、`uploaded-analysis-resume-regression.test.js`、`text-provider-stream-progress.test.js`、三模式真实上传/AI 选题提示词链、五分钟资产直投 H3 成片与 0.16.107 打包/安装态审计；最终历史素材成片另保留供应商偶发画内文字的真实边界 | 已验证 |
| BUG-098 | 同一镜头的对白时间已重排，但画面仍按旧分段重复说话或切错人；仅在对白中提到商品、头发上已有泡沫，也会多传商品包装图并挤占 H3 参考位 | 对白重排结果和画面时间线分别编译，提交层未执行单一时间轴合并；旧拼接器又按过短局部窗口裁掉已在付费原片中完整生成的句尾；商品参考判定混入对白、状态和派生商品字段，没有区分“包装真实入镜”与“口播/残留效果” | 0.16.107 | `prompt-reference-manifest-regression.test.js` 单一时间线、旧切镜折叠、染后结果、预涂泡沫、显式包装入镜矩阵；全量 674/674；五分钟 14 镜使用历史原始 H3 片段零新增付费重拼，54 句逐镜复核完整，整片 ASR 字符序列比由 0.658 提升至 0.8008 | 已验证 |
| BUG-099 | 本地人物、场景、商品或音色明明存在，H3 任务仍会持续运行一小时后超时，白占云端实例；恢复后再次提交还会复现 | 资产记录同时保存 `path` 与历史 `remoteUrl`，提交适配器错误地让远端地址优先；该地址属于 24 小时任务临时对象，过期后 H3 无法取回素材，但网关只能在一小时看门狗处结束 | 0.16.107 | 本地文件权威重传、远端-only 兼容、缓存身份改用本地哈希；`image-reference-upload-regression.test.js` 44/44、全量 674/674；同一 S26 从 3615 秒失败降至 456 秒成功，单次提交、实扣 ¥1.56、对白完整；0.16.107 本机静默安装与安装态审计通过 | 已验证 |
| BUG-100 | H3 单任务对白过碎或过密、错人读词；系统仍可能为当前对白先造音频，场景四视图在分块时被裁切，成片验收还依赖 ASR | “对白事实、音色样本、供应商任务”三层边界未统一：旧合同仍允许 2–5 轮问答，人物校准视频曾复用剧本台词，分块裁图条件误覆盖非分镜板参考，历史成片脚本又把 ASR 当最终准入 | 0.16.109 | 每个 H3 任务最多两句完整对白（同人连续两句或双人各一句）、第三句顺延；逐字文本直接写入 H3 且人物图拥有说话人/嘴型，音频仅为可选中性音色、不承载当前台词；场景四视图整张原图跨提交；移除成片 ASR；`h3-two-line-native-speech-regression.test.js`、`agent-director-contract.test.js`、全量 685/685、专项审计、打包态与安装态审计 | 已验证 |
| BUG-101 | H3 成片语气平淡、台词缺少前因后果、镜头缺少动机，同一人物或场景中的实体可能被复制成分身 | 最终 provider 编译优先取简短表演字段且会截成残句；人物参考被固定裁成两人、道具引用漏读稳定 ID 与英文时间线；身份参考把永久脸型与可变化发色/服装混为一体；原稿动作声未合并进最终声场 | 0.16.110 | 导演载荷 v21 将“前因→动作→逐句声线/表情/肢体→听者反应→结果”编译成唯一时间线；说话人机位所有权和有动机正反打/群像切镜；人物、场景、服装、商品、道具稳定一一映射与单实例合同；剧情换发色/换装状态优先；新增因果导演载荷 `4/4`、全量 `692/692`、构建资源审计、安装态版本与合同静态审计 | 已验证 |
| BUG-102 | H3 提示“人物身份图绑定重复或不属于本镜可见人物：C03”，合法第三人物被软件拦截 | 提示词引用规划已按剧情语义支持最多四名人物，但严格提交校验仍按旧的“两名主要可见人物”集合判断，导致编译器加入 C03 后被下一层误判 | 0.16.111 | 新增唯一权威 `shotReferenceCharacterIds`，提示词、确认清单、严格校验和实际提交统一人物集合；机位仍最多突出两张脸；专项 `5/5`、全量 `693/693`、25 镜真实 H3 单次提交与 300 秒整片通过 | 已验证 |
| BUG-103 | H3 五分钟链路出现台词缺失或错人、多人张口、服装状态串人、固定运镜、情绪平淡和商品成交信息不足 | 历史实测脚本另维护漏镜头/固定时长/对白覆盖；最终提示词重复模板稀释逐句表演合同；服装按镜头而非人物传播；运镜未读取剧情事件；音色与商品事实缺少兼容和成交语义 | 0.16.112 | 31 镜/363 秒/65 句唯一事实源；每任务最多两句；人物级服装状态；事件驱动运镜；逐句口型/情绪/表情/朝向；音色兼容；商品事实与 CTA；全量 `700/700`、五分钟零付费干跑、包内和安装态哈希一致 | 已验证 |

| BUG-104 | 对白丢失/错人、跨镜跳变、人物名存在但台词缺失，且部分入口绕过资产前提示词确认 | 原稿账本、镜头、Foundry IR 与中英文执行稿是多份可独立变旧的副本；独立剧本入口未复用统一确认门；运行态又污染确认指纹 | 0.16.113 | 唯一对白事实账本、结构化 IR 指纹、双语 canonical shot、跨镜承接、统一资产前确认、逐句对白审计；专项 `5/5`、相关 `57/57`、全量 `705/705`、UI 7 组/交互 `8/8` | 已验证 |
| BUG-105 | 资产前总弹窗或单镜窗口看不到提示词尾部，英文执行稿仍只有 500 字；语气偶发显示 `[object Object]`；内部批量入口可能先补依赖再要求确认 | 通用错误摘要清洗器被复用于创作提示词并压平换行、截到 500 字；全局 DOM 脱敏观察器又二次改写英文 `<pre>`；结构化语气对象被直接 `String()`；确认门只依赖 UI/IPC 而未落到三个批量工作流入口 | 0.16.114 | 创作原文保护区、中文/英文全文长度与尾标、对象语气扁平化、资产/分镜/视频工作流同门禁；专项 `4/4`、相关 `34/34`、全量 `709/709`、Headless UI 7 组/交互 `8/8` | 已验证 |

| BUG-106 | 剧情工作台生成的剧本与分镜视频提示词堆叠过多动作描写，对白、语气、表情和人物站位反而不突出 | 剧本策划、单元写作、导演 Agent、H3 编译器和逐秒合图五层分别要求“动作/表情/身体递进”，H3 规格又用全局英文词数下限判定过短，模型只能靠反复走动、转身、手部和家具动作填充 | 0.16.115 | 统一“对白优先、动作最小化”中英文生成合同；每句锁定说话人/听者、语气声调、表情、站位朝向和视线；每节拍最多一个必要主动作与一个闭口反应；取消全局提示词长度下限和固定三段动作；专项 `7/7`、相关 `98/98`、全量 `716/716`、打包态与安装态审计 | 已验证 |
| BUG-107 | 用户要求的中文 `integrated_multimodal_description` 时间轴只停留在示例层，实际确认稿和 H3 提交稿仍可能沿用旧英文六段式；无显式时间码时，回应对白还可能跨入群众反应镜 | 最终展示序列化、提交序列化、旧缓存与导演计划各自维护格式；导演中文动作/声音/状态字段在生成块重绑时丢失；通用英文关键词翻译把朝向误成表情；旧对白调度只按剩余秒数移动分镜边界，没有优先使用“某角色回应”的原稿镜头证据 | 0.16.116 | 展示稿与提交稿统一为同一份中文毫秒时间轴；完整参考绑定、逐句说话人/音色/口型/听者闭口、专用换装优先、直视观众和自然静默尾段；对白优先绑定原稿分镜语义再分配时间，不跨入群像镜；旧缓存本地重编译；全量 `722/722`、UI 7 组/交互 `8/8`、打包态/安装态和唯一一次真实 H3 单任务验收 | 已验证 |

| BUG-108 | 上传剧本已完成 AI 标准化，却报 D001 起整串说话人与原稿不一致 | 标准化编译先按显式人物表分配角色 ID，规范人物清单随后按首次说话顺序重排 ID；对白重绑定错误地优先信任旧 `speakerId`，使林曼秋的 C02 在重排后指向哈桑、苏晚晴的 C09 指向礼宾主管；0.16.122 复验发现镜头对白已按姓名恢复，但持久化不可变账本仍在最后一步先写旧 ID，重新打开真实项目后会复发 | 0.16.117（持久化复发补齐 0.16.123） | 不可变对白账本及镜头对白都以原稿说话人姓名为事实源，旧 ID 只作缺名兼容；新增持久化账本 ID 重排断言；真实失败项目须再次完成 105/105 零错配复验后方可进入付费媒体阶段 | 已验证 |

| BUG-109 | 人物资产提示词显示台词、画外与背景人物和大量普通物品被重复建资产；音色库性别年龄标记与实际声音不一致 | 角色说明、对白和视觉外貌共用 `description`；说话人/听者被默认视为需建模人物，单句出镜龙套也会越过资格门；物品台账没有核心连续性准入；音色标签完全信任历史人工字段，没有读取 WAV 声学特征，也没有稳定的内置音色基线；0.16.120 生产计划已跳过非资产对象，但提示词确认页仍错误枚举它们 | 0.16.120（提示词确认页补齐 0.16.122） | 独立人物/物品资产准入与跳过原因、单句出镜/反应背景板排除、外貌字段去台词污染、性别年龄主配角元数据自动入库、历史无效选中绑定自动停用、提示词确认与生产计划同一准入、WAV 声学核验与错标修复、40 个独立可试听内置音色；专项回归、全量回归、真实项目迁移、打包/安装/重启及多分辨率 UI 审计 | 已验证 |
| BUG-110 | 软件启动、切换页面和打开大资产库明显卡顿 | 启动时重复反序列化 91 个历史项目及 SQLite 全量快照；40 个内置音色每次启动重新哈希和声学审计；页面切换重新读取整项目，资产库一次构建数百个卡片 | 0.16.121 | Foundry 轻量状态头、已迁移合同快检、内置音色跨进程缓存、项目基线单次克隆、本地页面切换、资产/候选/音色分批渲染；启动性能专项、全量回归、0.16.122 打包/安装态真实数据性能复测 | 已验证 |
| BUG-111 | 资产直投项目已从资产库选中合适的人物四视图，后台批次仍把同一人物列为缺少身份图并重复付费生成 | 批次计划只检查 `character_intro`，没有复用当前制作版本已选中的 `character_sheet` / `character_three_view` 统一人物身份候选 | 0.16.124 | 资产直投人物准入改用统一身份候选解析器；当前版本选中的任一有效身份图都跳过重绘；新增四视图跨阶段复用专项回归并在九宝茶真实项目预检核对 | 已验证 |
| BUG-112 | 同一人物音色在并发分镜预检时重复标准化，确认人物图后已绑定的独立音色又可能被误标过期；运行态音色记录还会让已确认提示词反复失效 | 音色标准化没有单飞锁；依赖失效把全局音色库当成人物图派生资产；提示词指纹混入运行态音色绑定与供应商计划 | 0.16.125 | 同一人物/源文件/时长标准化单飞；独立音色不随人物图失效；运行态字段退出提示词事实指纹；专项并发、缓存稳定性与 40 内置音色回归 | 已验证 |
| BUG-113 | 普通道具被错误绑定到每个分镜，确认项目资产后全局资产库卡片仍显示未入库；并发图片任务偶发拿到相同上游任务号串图 | 道具 ID 被截成通用 `pro` 子串；确认后只复制文件不回写双向链接；图片提交缺少唯一请求身份和任务归属碰撞门 | 0.16.125 | 道具精确 ID/语义别名绑定、仅核心道具准入；项目候选与全局库双向链接；并发图片唯一幂等键与跨对象任务碰撞拒绝回写 | 已验证 |
| BUG-114 | 用户要求每个分镜只生成一次，但瞬时错误仍可能触发外层重试，重启后也可能再次越过提交边界 | 分镜批次的重试器与上游任务恢复没有明确区分；提交尝试次数未在跨越上游边界前持久化 | 0.16.125 | 严格一次模式在提交前持久化尝试计数；每个确定性 H3 生成单元最多一次上游提交；已有 taskId 只查询/下载；质量重抽与瞬时重试全部关闭；网络与断点恢复回归 | 已验证 |
| BUG-115 | 多段 H3 分镜只成功一部分时，界面仍把最后一段显示为完整分镜，自动选中并进入资产库，导致完整率和最终合成虚高 | 内部连续生成块完成后被提升为普通 `shot_video` 候选；完整性判断没有核对导演计划中的全部块 ID | 0.16.125 | 内部片段永不自动选中、确认或入库并从资产 UI 隐藏；只在全部计划块按 ID 齐备后本地合成完整分镜；九宝茶真实项目从虚报 41/42 校正为 40/42 | 已验证 |
| BUG-116 | 已经生成成功且音色没有变化的完整分镜，重新打开项目后被误判过期，导致本可复用的成片不再计入完成数 | 音色血缘复核按父分镜统计说话人总数；父分镜有三人说话时，即使每个 H3 实际生成块均满足最多两种音色，仍会被旧规则整体判为超限 | 0.16.126 | 音色血缘改为逐个供应商生成块审计；父分镜允许多于两名说话人，只要每个付费块不超过两种音色且当前 WAV 哈希与多模态血缘一致；九宝茶真实项目恢复 11 个合法历史成片并新增三说话人/两音色分块回归 | 已验证 |
| BUG-117 | 分镜已有完整成片，但同镜头后来产生的内部 H3 片段可能在下载兜底中被当作最终视频，触发 `SHOT_VIDEO_INTERNAL_BLOCK_NOT_SELECTABLE` 并中断全链路 | 首次下载兜底直接在全部 `shot_video` 候选中取最新记录，绕过了完整候选筛选器，导致时间更新的内部片段覆盖较早的完整成片 | 0.16.126 | 新增唯一完整分镜候选解析器，所有兜底显式排除内部块、不完整块和不可选候选；新增“更新内部片段不得覆盖完整成片”回归；九宝茶 42/42 成片与 472 秒整片解码通过 | 已验证 |
| BUG-118 | Codex 资产包明明走了导入入口，项目却显示“资产直投”，包内镜头级参考又被当成“首帧”，导致用户无法确认实际执行模式 | 0.16.127 的导入器把资产包项目硬编码为 `asset_direct`，并把 `shot_anchor` 候选落成 `storyboard_start`；工作流和 UI 没有独立的资产包生产合同 | 0.16.128 | 新增独立 `production_package` 模式、`shot_anchor` 血缘、旧项目确定性迁移、包内剧本/资产/提示词只读锁、无音频/无上一镜视频合同和每镜最多一次上游提交门；专项 `8/8`、全量 `771/771`、模式选择与安装态真实包导入审计 | 已验证 |
| BUG-119 | 打开短剧老虎机后周期性卡死，项目无法选择，点击删除没有反应 | 主进程每 15 秒为自动恢复遍历项目索引，却在判断是否需要恢复之前读取、迁移并深拷贝所有完整项目；94 个真实项目中包含 19.7 MB、13.6 MB 等大型快照，历史失败项目也被全量恢复扫描，事件循环被长期占满，实测主进程从约 365 MB 增长到 2.6 GB 并进入未响应 | 0.16.131 | 自动恢复与脱离任务协调先在轻量项目摘要中过滤，只加载真实可恢复或仍拥有活动视频任务的项目；稳定历史失败项目按需懒检查；项目选择与删除立即显示忙碌状态；新增 302 项目、17 秒跨定时器、切换和可恢复删除的隔离压力审计 | 已验证 |
| BUG-120 | 导入 Codex 资产包后抽卡连续报错；暂停时未拿到 taskId 的分镜被显示成红色“幽灵任务”，随后又因“每镜一次”锁无法继续 | “本地上传/传输尝试”和“上游已创建生成任务”共用了 `submissionAttemptCount`；页面恢复还把所有活动且无 taskId 的任务直接判死，忽略 20/30 分钟保护期与运行中的操作；暂停信号在 HTTP 请求前已生效时仍被当作响应未知；Agent 边界丢失 `noRemoteTaskCreated` | 0.16.132 | 新增 `not_started/preparing/not_created/unknown/created` 上游状态；页面刷新只清理真正超龄任务，显式停止才强制释放无 taskId 占用；HTTP 前暂停明确为未创建且未扣费；响应未知只用原幂等键恢复同一任务；资产包按钮区分“已生成一次”和“继续原任务”；旧误判记录自动迁移；专项 59/59 后执行全量、打包和安装态验收 | 已验证 |
| BUG-121 | 上游 H3 完全没有收到任务，下游仍显示“后端任务正在生成”并持续转圈 | UI 将本地上传/提交准备与已取得上游 taskId 的生成任务合并统计；提交计数和费用占位又在真正 HTTP 请求前提前落盘 | 0.16.133 | 上传、提交中、上游已创建三态隔离；仅有效 taskId 可显示生成中；HTTP 请求前后双重暂停门；请求开始事件作为唯一提交边界；缺 taskId 响应保持未知且禁止重提；47/47 专项回归、真实九宝茶项目 16 条幽灵任务修复、打包与安装态审计 | 已验证 |
| BUG-122 | 点击继续后长期显示“本地提交准备/正在上传”，界面严重卡顿，最终上游仍无任务 | 资产包把多张分散人物/场景图按 `reference_to_video` 提交，但官网官方 H3 的无音频路径只接受单镜锚点/首尾帧；客户端未在上传前校验，参考上传还被硬限为3路，16个分镜同时反复加载和序列化4.62MB项目，主进程实测达到约2.7GB；重启后又把已终止的无taskId调用保活20分钟并逐条重写整项目 | 0.16.136 | 上传前验证官方无音频合同；旧多资产板包立即失败并显示未创建/未扣费；单张 shot_anchor 映射官方 image_to_video；超过10秒提前拒绝；合法任务保持16路提交并把素材上传放宽到16路且按内容去重；上传重试耗尽及重启后无taskId调用立即转暂停/待恢复；同项目恢复合并为一次落盘；全量回归、打包态与安装态审计 | 已验证 |
| BUG-123 | 资产包被错误要求“镜头锚点图”，管理员即使拥有官网管理员身份仍被本地和租约数字并发排队；未创建上游任务的历史记录还被显示为红色失败 | 0.16.136 把 AutoDL 官方多参考图工作流的可选音频误读为必填，于是把多图无音频错误降级成首尾帧并发明了 `shot_anchor` 前置条件；客户端批处理又没有消费官网签发的管理员身份，sidecar 也继续对管理员套用套餐数字上限；状态层未区分“生成失败”和“上游从未创建” | 0.16.137 | 资产包按锁定顺序直接提交 1–9 张人物/场景/核心物品/商品图，音频为空仍走 `reference_to_video` 且支持 1–15 秒；管理员本地批次不设上限、服务端租约不按数字拦截但仍逐任务鉴权；未创建且未扣费的稳定请求显示为可继续，不计失败；普通账户限制不变；真实 70 镜/582 秒项目零提交预检、客户端全量回归、sidecar 3/3、官网 r152 构建及 14 秒无授权零扣费路由探针通过 | 已验证 |

## 本次全链路验收新增记录

| 编号 | 现象与根因 | 修复版本 | 回归与状态 |
| --- | --- | --- | --- |
| BUG-124 | 源稿拆解把“不出现字幕”误判为“出现字幕”，连续无效重写；报错还夹带上一测试剧的外卖迟到事实 | 0.16.190 | 按局部指令区分肯定/否定，转折后的新增文字仍拦截，报错引用实际违规句；真实第12批原输出经完整校验复用、零次新增调用。1205项测试：1204通过、1跳过；121文件安装态一致，57项目与34凭据未变。已验证 |
| BUG-125 | 视频审核只看证据图片路径存在，2字节中断缓存也被当成可读图；未完成文件可能被并行审核提前读取 | 待发布 | 派生图片写入独立临时文件、检查完整首尾标记后原子发布；截断图重建、JSON缓存原子保存，原始素材不变。四路并行与损坏缓存恢复通过；待全量和打包/安装复核 |
| BUG-126 | 方括号人物/场景资料被纳入对白；按文本全局去重会漏掉另一次真实发声；动作叙述可能被解析成长人名 | 待发布 | 按资料段落边界排除元数据，按源位置区分重复发声，正式括号说话人优先于听者，已知人物动作不创造新说话人。新剧前18批1329个说话人绑定字符逐字同序；专项通过，待全量和打包/安装复核 |

## 维护规则

本次验收补充（尚未安装）：BUG-127，道具出现在商品镜头便被归为商品部件，同时源稿道具别名/位置修饰未独立核对，漏掉旧照片等证据。已取消“同镜出现=商品部件”，新增原稿道具逐项处置与可见/收存出场依据，保留真实历史资产；1213项回归中1212通过、1跳过。BUG-128，分批资产设计未读取已定布局，导致同一走廊东西方位相反，人物身份图混入手持道具；已补跨批设计上下文、人物与道具分离、开场家具状态和定点修复指令。专项6/6，实际Agent修正及新包安装待验证。

2026-09-06T14:36Z 补验：0.16.193 已覆盖安装。全量1215项中1214通过、1跳过、0失败；122代码文件与安装态一致。原57项目稳定字段无变化，34凭据一致，活动业务任务为0。BUG-125/126/127的源码及打包安装回归通过；新剧20段1543个说话人绑定台词字符同序。BUG-128的规则及安装回归通过，实际资产设计定点修正仍在执行。以上不代表新片图片/视频/剪映全链路已完成。

- 每个修复都递增最小单元版本号。
- 不删除历史条目；复发时在原编号追加复发版本和新根因。
- 官网钱包是余额、冻结金额、充值订单与到账状态的唯一权威，桌面端不维护独立余额。
- “已验证”必须同时具备源码回归用例和打包后检查；涉及官网的条目还必须保留线上或候选接口验证结果。

2026-09-06 本轮补充（安装验证前不得标为上线）：

- BUG-129：`syncReferenceLibraries` 用“老照片”等通用别名合并了不同稳定 ID，地质队合影被满月照覆盖。稳定 ID 优先，源台账及导入包 P01/P02 也不得作为可随意重排编号；完成记录增加真实 ID/名称核对。实际项目从已保存的第285次结果恢复两张独立照片，三次同步保持独立、正确满月照设计未重做，合影第290次独立设计已完成；尚无新图片生成。稳定编号与台账回归9/9。
- BUG-130：尚未精修的部分导演草稿，先按正式请求检查10228字符，触发10000字符上限并中断整轮。区分草稿与最终请求；保留待修订分镜，不提高上游上限、不截断，不允许漏掉的视频项目因其余卡片全确认而误获批准。实际41镜隔离项目零模型调用复现已恢复为可编辑待完成状态；单项/全部确认回归2/2。
- BUG-131：导演文字超时后仍翻译旧预览、审核无法提交的中间结果，徒增等待。未完成批次在同一所选Agent配置下从最多5缩为2再缩为1，已完成输出保留；导演未完成时不再编译/翻译旧版视频与静帧预览。单镜仍失败时保留明确断点，不无限即时重试。两项专项回归通过。
- 0.16.194 为中间构建，122代码文件打包一致，1222项测试1221通过/1跳过/0失败；未安装。BUG-131合并至后续0.16.195，完整回归、打包和安装复核单独记录。当前真实视频与剪映交付仍未完成，不得以文字通过冒充成片通过。
- BUG-132：已审定的双语资产设计又被整段自由翻译，真实审核发现 ajar/虚掩被改成半开。现对完全匹配的标准资产提示词复用双语固定版式及已审中文设计；手工改动英文后不复用旧查看稿。英语生产请求不受中文显示修复影响；新增双语状态与手工变更回归。
- 2026-09-06T15:51Z 补验：0.16.195 已覆盖安装。最终1225项测试1224通过、1个历史报告图缺失用例跳过、0失败；122代码文件源码/包内/安装态相同。原57项目关键字段无变动、34凭据一致、无活动业务任务。备份 preinstall-1788709581855 的两份SQLite均 quick_check=ok。安装包SHA256：97F37E036A718229F35B7763917DC66B3983EC4ED949D31E045967C0952EB326。BUG-129/130/131/132的上述源码和安装验证完成；真实新片图片、视频、实际视频终审及剪映交付仍未完成。
- BUG-133：已有可查看的部分提示词审核卡片时，重新请求审核只打开旧卡片，没有继续生成仍标记 pending 的分镜。入口现在检查未完成的编译状态并恢复准备流程；原稿、已完成输出和手工审核仍保留。新增恢复用例通过。
- BUG-134：源资产台账保留集合与成员关系，但导演编译输入丢失 parentId，造成实际第二镜把一套笔记本编号当成多个集合描述。导演输入现保留关联集合、成员 ID 与既定外观，规则明确成员拆离后总量不增加；不向其他剧本注入本剧数量或物品。无关联道具的输入保持兼容，新增两项回归通过。当前主剧因语义输入更新正重新编译，尚不能声称实际生成画面已验证。
- 2026-09-06T16:22Z 补验：0.16.196 已覆盖安装。最终1228项测试1227通过、1个历史报告图缺失用例跳过、0失败；122代码文件源码/包内/安装态一致。原57项目关键字段不变、34凭据一致、活动业务任务为0；备份 preinstall-1788711263183 两份SQLite quick_check=ok。安装包SHA256：5EB19CCE52F7879F37C5AAA2F7EF78D78D6A08B6ADDA17660AFDF7D855F7F940。BUG-133/134已通过上述源码及安装回归，Skill已同步；新片真实资产和剪映交付尚未完成。
- BUG-135：单镜导演请求仍重复携带全剧212条道具出场记录，单次用户消息152791字符，其中道具台账136366字符；大部分是本批不会执行的动作重复。新增批次上下文投影：当前出场逐字段保留，加最近前后出现及集合成员关系；不改完整规范源、不截断句子、不改变Agent配置。41镜平均正文由152185降至27075字符，当前出场逐条相等；实测速度另行验证，不以字符减少冒充时延提升。
- BUG-136：提及道具即以叙述中第一个人名、唯一可见人物或提及该物的说话人推断持有人，真实第一镜的内袋汇款单被错误标给C01，原动作实际属于C03。移除无依据的持有人默认值及其跨镜继承，明确绑定不变；缺失归属由导演按完整原动作与手/袋/桌/地面支撑关系解析。新增三类误归属与显式绑定回归，Skill同步。
- 2026-09-06T16:45Z 补验：0.16.197 已覆盖安装。1231项测试1230通过、1个历史报告图缺失用例跳过、0失败；123代码文件源码/包内/安装态一致。备份 preinstall-1788712887438 两份SQLite quick_check=ok；57项目关键字段未变、34凭据一致。安装包SHA256：201D2B94C0800C07791C10F6FC6709A3624E60A30EC9C0536BC8B9524BD38166。BUG-135/136源码与安装验证完成；新输入下的模式测试恢复中，不代表实际新图、新视频或剪映草稿完成。
- BUG-137：真实主剧第310次文字返回保留了完整第一镜，却因外层JSON未闭合而没能推进。向上追踪发现AdaptiveProductionAgent错误包装白名单遗漏rawText，恢复器拿不到原文；重启恢复仅匹配不带编号的旧操作名，实际操作名带 `_1`、`_2`。错误层现保留原文及长度/哈希/截断标记；恢复器支持编号批次，并校验当前项目与源指纹对应的会话前缀，拒绝旧源稿回复。新增测试先复现2项失败，修复后包括真实工作流边界及旧源排除的6项专项均通过。真实已保存5826字符回复在内存隔离探针恢复S01，下一次只请求S02，新增Agent调用0、媒体调用0、原项目未写入。完整回归1235项1234通过、1历史图片缺失跳过、0失败。0.16.198已构建，安装复核另记。
- 2026-09-06T17:17Z 补验：0.16.198 已安装，123文件源码/包内/安装态一致；57项目关键字段不变、34凭据一致、活动业务任务0。备份 preinstall-1788714683739 两份SQLite quick_check=ok。安装包SHA256：6BFA39D59CF67E7C685701F7C53DF13015F2BFAD1111BF9A1A206D882F3FF2B4。实际恢复后主剧S01直接复用并请求S02；分镜合图S03/S04直接复用并进入S05/S06，未重复调用这三镜。新图、新视频、实际终审及剪映交付仍未完成。
- BUG-138：实际参考图预案把内袋中未露出的汇款单及夹存照片当作可见物，S02因此多传到10张；S20–S22托握原装罐却因全名被分隔/省略而漏传商品图。向上追踪：选择器只匹配文字提及，未读取已审核的 visible/stored 出场台账；预览与提交又分别通过 propNames 回退绕过选择器。现共用可见物选择结果，当前台账需源指纹和精确出场证据有效，同镜先藏后取保留，过期台账不压制新动作；包装全名规范化并识别明确绑定原商品的原装包装拿持动作。专项先复现4项失败，修复后7项通过；实际S01无隐藏单据、S02为9张、S20–S22均有原商品且无隐藏证据物。规范源指纹未改变，不重做已编好的分镜。Skill同步，完整回归及安装复核待补；不代表新视频已生成或验收。
- 2026-09-06T17:41Z 补验：补入商品留在已建立台面及触碰已观察包装部件的承接用例，专项8/8。最终1243项测试1242通过、1历史图片缺失跳过、0失败；直接Node测试进程exit0。0.16.199覆盖安装exit0，123文件源码/包内/安装态一致；57项目关键字段不变、34凭据一致、活动业务任务0。备份preinstall-1788716089378的两份SQLite quick_check=ok。安装包SHA256：3F9247609A73CC8E57583D9DF59034695FF345C60A0B8671F67E01920D036A71。41镜实际参考图只读检查通过，最多9张，源指纹不变；新图、新视频、实际终审及剪映交付仍未完成。
- BUG-139：官方精修稿已逐字绑定对白和时间，但资产包构建器/导入器仍要求旧版逐行 at least/Only...moves the lips、hard cut 等固定短语及重复的 criticalActionEn/actionBeats。真实8镜提示词原样预检全部被旧合同拒绝。新增与Skill共享的官方精修合同：保留完整源镜、源动作时间线和精修记录，重新验证源指纹/对白表/语速/动作覆盖/人物/图片引用，再用同一正式编译器按实际有序图片独立重编译，要求英文提示词完全相等。新合同不再靠旧短语判断；旧包路径与三层审核/图片字节校验不变。新增篡改说话人、声音、图片身份、时间、动作台账和中文对白反例；Skill真正运行构建器生成测试包，再走应用导入并核对精修源指纹和英文不变。专项18/18，其中测试图片明确为1像素程序夹具，不是本剧媒体验收。候选变更未安装时不得声称当前安装版兼容新合同。
- BUG-140：实际改写测试8镜视频稿已通过，但现场照片资产仍被审核拒绝。根因一：通用设计清理要求移除人物/动作，误删相纸内部原有影像，只将其留在不提交生图的designChoices。根因二：生产审核自动修复仅覆盖视频/静帧，资产形象的负面审核没有回到设计阶段。现按资产类型区分外部演员与固有印面；完整原设计保留历史，未生图且提示词仍为当前系统稿时，来源明确的负面审核进入所选规划Agent的有界定点设计修复并再次审核。手动稿、已有真实图片不被该文字清理覆盖。新增3项先失败后通过；资产设计/实体模板专项12/12。Skill同步清理新旧打包格式的重复指令矛盾。尚待真实Agent修订结果、完整回归及安装验证，不代表新资产/视频通过。
- 2026-09-06T18:44Z 补验：0.16.201已覆盖安装，124代码文件源码/包内/安装态一致；1250项测试1249通过、1历史缺图跳过、0失败；57原项目关键字段不变、34凭据一致、活动业务任务0。安装包SHA256 B52C7ECB51238A5AA897A026047FCB8DF486D17C4817C09C34694A5D7FF02C8F。BUG139打包合同及BUG140修复路径已安装，真实资产修复审核因后续Grok账户错误尚未完成。
- BUG-141：Grok连续两次约69秒无正文被显示为本地超时。最小JSON探针保存的真实终止事件为errors数组：HTTP402 Payment Required、Grok Build usage balance exhausted，duration_api_ms=0。根因是分类器只读取error单值、不读errors数组，然后扫描含工具timeout说明的启动原文，错误匹配超时。现优先读取结构化终止错误数组，识别402/429/余额不足；任务记录保存errorCode，界面明确提示余额/额度检查及保留断点，不切换API。两项回归先失败后通过，含真实Node子进程非零退出边界；结合集成专项20/20。不会为验证修复继续调用余额已耗尽的Grok。完整端到端任务因审核源账户容量不足仍未完成，必须补充额度或由用户授权替换审核源。
- 2026-09-06T19:03Z 补验：0.16.202覆盖安装完成，124代码文件源码/包内/安装态一致；1252项回归中1251通过、1历史缺图跳过、0失败。57原项目关键字段未变、34凭据一致、活动业务任务0。备份preinstall-1788721227549含68文件及两份quick_check=ok的SQLite快照。安装包SHA256：142349EF6D1C31BEDD681613835BB3740F73349E7D5186CF7F2AE33F192242EC。外部Skill格式校验通过，共享提示词审核规范与应用内副本字节一致。BUG141代码及安装验证完成，不表示Grok额度恢复；未继续尝试已明确402的账户。主剧已有19/41结构化输出但未完成终审；其他模式保留断点。尚无本次新图片、新视频或新剪映草稿，完整E2E未完成。

## 0.16.212 存储迁移与回归（2026-09-08）

- 工作台及两模式上传暂存统一跟随持久素材目录；已有数据优先，新 Windows 安装有 D 盘时优先 D 盘，保存配置失效时不创建空 C 盘替代目录。
- 本机 3,822 文件迁入项目“运行数据”；C 旧路径目录联接保留，44 组/87 份不可变素材完成物理去重，内容及修改时间均相同。
- 1,339 项全量测试：1,338 通过、0 失败、1 历史缺图跳过。已覆盖安装 0.16.212，125 个代码/界面/配置文件包内及安装态与源码一致；实际暂存写入 D 盘并清理。
- 59 项目、原 1,477 资产和 94 音色 ID 保留；正常元数据补录后资产库 1,493。1,006 候选指纹相等，3 个数据库 integrity_check=ok，17 个凭据解密后值相同；历史原有 3 条音频缺失引用未扩大。
- 仅保留 3,390,039,370 字节软件/关键数据回滚包；媒体依赖现用 D 盘并逐文件校验，不属于独立素材灾备。新增全量临时备份已按联动清理任务明确授权移除。
- 安装器 SHA-256：36D938D5665D0DE189A9EBDBCBA9554A6F723E3DBB6440331786B0E666ECFC87。完整结果见仓库根目录 0.16.212-D盘素材迁移与后台验证报告-20260908.md。

## 0.16.213 商品图导入无响应（2026-09-08）

- 已确认本地图保存后选择 IPC 仍等待可选公网暂存；移除此等待和第二次商品覆盖，实际生成继续按需解析参考图。
- 补齐导入中状态、重复点击防护、取消/异常恢复和切换项目响应边界。
- 全量 1,347 项：1,346 通过、0 失败、1 历史缺图跳过；新增专项 8 项。
- Electron 对真实用户 PNG 解码、复制、持久化、入库和协议返回均通过；安装归档导入约 371.8 ms，未调用云端。内置浏览器组件 6 项交互和 axe 扫描通过；未进行系统 GUI 自动化。
- 0.16.213 已静默安装；59 项目、1,006 候选指纹、原商品及库索引保留，17 凭据有效值相同，3 SQLite 完整性正常。
- 安装器 SHA-256：3C1DB18998F7B820DB1D296AD7EEA9B2F2D9851FD1A0125584FCE81C100A8EF8。详细报告：仓库根目录 0.16.213-商品图导入无响应修复报告-20260908.md。

2026-09-08 21:03：TASK-20260908-PREPRODUCTION-15MIN-214 / TASK-20260908-DRAMA-016214-WEBSITE-001 已发布官网 r157 / 0.16.214。1375 项回归通过，0 失败；190 个源码与包文件一致；后台商品图导入通过；真实单镜提示词独立审核通过。公网完整下载校验通过，SHA256 EE5B6C0868874B4581C32CDABA5E42DE09A0407A86DA078A7D8E9E5F883E8B39，138772077 字节。原用户项目保持暂停，本机安装版未覆盖。全流程十五分钟成功验收仍未通过，不得记为整体目标完成。官网旧 r156 和精确发布前备份保留，未覆盖数据库。详情：D:\Backup\Documents\无限画布\纯梦短剧老虎机\.codex_tests\TASK-20260908-PREPRODUCTION-15MIN-214\REPORT.md。

2026-09-08 21:15：本机覆盖安装 0.16.214 完成；233 个程序文件与发布包一致，87 个用户项目/配置文件无变化，后台商品图导入通过。整稿拆镜真实请求仍 330 秒无正文超时，全片十五分钟验收未完成。详见 D:\Backup\Documents\无限画布\纯梦短剧老虎机\.codex_tests\TASK-20260908-PREPRODUCTION-15MIN-214\REPORT.md。


## 0.16.241 · 2026-09-11

修复 Agent 复杂结构返回停滞、导演秒数执行冲突、用户台词数量漏审、后选参考资产缺设计、原稿题字/界面文字中英文缺失、AG 审核冗余传输与空回执，以及等价时间别名导致的缓存失效。WorkBuddy 模型目录合并版本缓存并标明来源。真实 4/3/3 剧本项目修复后 10/10 通过，28 镜、87 条提示词；重复准备全部零新增模型请求。安装版本与80个关键源码文件一致，72份项目JSON未变。回归1583通过、1跳过、1历史外部样本缺失未通过；不据此宣称长期95%首次通过。详见 .codex_tests/TASK-20260911-REPAIR-241/验收报告.md。

## 2026-09-11 · AG 原会话回收与发送前网络恢复（0.16.243）

已安装及官网发布。CLI print 到期后保持原会话并读取已完成步骤，不重复生成；零轮 Eligibility TLS 失败同模型恢复一次。专项 20/20；新项目剧本与 9 项提示词最终通过。全量 1592 通过 / 1 跳过 / 1 外部历史样例缺失失败，未掩盖失败。详见 ../../.codex_tests/TASK-20260911-AG-242/验收报告-0.16.243.md。
