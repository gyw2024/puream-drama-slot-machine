# 纯梦短剧老虎机 0.16.92

- 修复文本供应商下拉框厂商名被错误替换的问题，官网内置 GPT / Claude 现在清晰可见。
- Gemini 连接测试改为只读取模型目录，不再占用生成请求额度。
- 文本模型 429 限流按上游恢复窗口自动续接，同一任务、同一会话、同一幂等键保持不变。
- 日配额耗尽采用有界恢复并保存断点，不会无限循环调用；已有输出或回执的请求不会整段重放。
- 限流等待状态显示预计恢复时间，不再把原始上游错误直接暴露给用户。

本版本仅生成本地安装包并更新本机，未发布到官网。
