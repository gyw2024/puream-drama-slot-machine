# 纯梦短剧老虎机 0.16.126

本版本把九宝茶真实全链路测试中暴露的两处复发性问题修到共用生产逻辑，并完成全量回归。

- 音色血缘现在按实际 H3 生成块核验，不再因为父分镜总说话人数超过两人而误杀合法历史成片。
- 分镜视频兜底选择统一排除内部生成片段和不完整候选，完整成片不会再被更新的中间片段覆盖。
- 九宝茶真实项目完成 42/42 分镜、472 秒有声竖屏整片合成和全片解码。
- 严格一次审计保持生效：每个实际供应商任务的提交尝试次数均为 1；原有终态失败只在用户明确授权后建立新的替代任务。
- 全量自动化测试 751/751 通过。

根因范围：`app/workbench-workflow.js` 的 H3 音色血缘复核与完整分镜候选选择逻辑。
