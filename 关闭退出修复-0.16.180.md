# 关闭退出修复 0.16.180

任务：TASK-20260905-DRAMA-CLOSE-180。范围仅限本地应用退出与 MCP 重启边界；不涉及官网、模型生成、项目内容、账号或并发权限。

## 根因与修复

1. `window-all-closed` 在检测到活动任务时调用 `createWindow()`，直接违背用户退出操作。改为记录用户退出并正常退出，不以活动状态阻止关闭；既有任务 ID 和启动恢复流程保留。
2. 旧回归测试反而要求上述错误行为必须存在。改为区分用户退出和 GPU 崩溃；GPU 崩溃期间保护任务，用户关闭不重建窗口。
3. MCP 连接失败会自动启动桌面端。新增无密钥的用户退出标记，正常手动启动会更新标记；同时在桌面启动入口防止旧 MCP 子进程重新拉起。
4. 网关重复关闭不得擦除用户退出标记，启动监听与关闭的竞态不得重新写入有效连接。

## 验证

- 全量：1029 项，1028 通过，0 失败，1 跳过。第一次全量保留在 full-tests.log，暴露了要求错误行为的旧断言；最终结果在 full-tests-final.log。
- 8 项退出专项：空闲、活动、失败、等待上游回执、headless 服务、退出时网关异常、真实 IPC 断开/重连、主进程接线。
- Electron 43.2.0 非 GUI 生命周期实测：加载打包 ASAR 的策略与网关，触发关闭事件后到达 will-quit，退出标记有效，没有创建窗口。
- 不使用原生 GUI 自动化；未执行鼠标点击关闭按钮的桌面验收。本次没有修改界面布局，不能将上述测试描述为已人工点击验证。

## 证据与回滚

证据：`.codex_tests/TASK-20260905-DRAMA-CLOSE-180/`。
基线：`.codex_backups/TASK-20260905-DRAMA-CLOSE-180/baseline/`，含修改前源码和已安装 0.16.179 ASAR。
安装包：`work/xiangsu-seedance-bridge/dist-fixed-0.16.180/纯梦短剧老虎机-安装版-0.16.180.exe`。
回滚使用保留的 `dist-fixed-0.16.179/纯梦短剧老虎机-安装版-0.16.179.exe`，不要单独替换 ASAR（完整性元数据必须与 EXE 一致）。
官网未发布；未提交大量已有工作区改动到 Git。安装态版本、哈希及数据变化以同目录最终验收文件为准。
