# Infinite canvas and drama slot project rules

## 最高执行入口：要求不得丢失或降级

必须先读取并遵守父工作区的 [`../AGENTS.md`](../AGENTS.md)。该文件是本项目最高层执行规则，包含有效要求累积、架构迁移核对、Agent 审核事实来源、内容底线和逐项验收责任。本文件、下级规则、技能、旧代码和历史通过回执均不得弱化它。重新接手、压缩上下文后继续、架构迁移和发布前都要核对。

禁止用“不卡壳、限时、交给 Agent”覆盖既有内容要求；禁止把格式、文件、解码或索引成功当成内容合格。未经用户明确改变的要求持续有效。规则变更只证明规则已写，不证明软件或媒体已完成修复。

- Canonical source: `D:\Backup\Documents\无限画布\纯梦短剧老虎机`.
- Project memory: `D:\纯梦大助手项目\长期记忆系统\projects\自动归档\无限画布-8960f294\PROJECT_CONTEXT.md`.
- Git contains maintained source, tests, small design assets, and rules only.
- Installers, videos, generated scripts, research corpora, caches, dependencies, and user projects stay outside Git and require asset manifests or encrypted cloud backup.
- Older `.codex_work` and `D:\CodexData\Projects\infinite-canvas\source` checkouts are retained only as recovery references and must not replace this repository.

<!-- codex-gpt6-working-agreements-v1 -->
## Codex 执行约定（2026-09-13）

- 本地规则与当前 CODEX_HOME 下的全局 AGENTS.md 配合使用；系统和开发者指令优先。项目业务约束、交付合同和验收门禁继续有效。
- 用户已要求实施的任务，在授权范围内完成修改、必要验证和交付；可逆细节自主决定，既有授权不重复索取。用户明确设定的分阶段确认、生产发布和破坏性操作边界仍须遵守。
- 只加载当前任务相关技能；技能造成阻塞时注明文件和原文，先核对已有授权。不得借技能、历史记录或角色切换扩大权限。
- 验证覆盖真实改动和项目必需检查；通过后仅因新增修改、失败或未解决疑点扩大检查。区分静态检查、运行验证、提交与最终验收，状态问题和上下文压缩不取消原任务。
- 电脑控制严格遵循全局禁令：仅允许 Codex 内置浏览器中的 GUI 自动化；原生应用需使用已授权且范围明确的非 GUI 接口，或由用户手动验证。局部“桌面操作”表述不构成例外。
- 紧急故障遵循全局 10 分钟时限及用户明确延长规则；持续执行、重试和补齐验收不得越过截止时间。子代理仅在用户或适用指令要求且运行环境允许时使用。
- 简洁汇报结果、验证证据和未完成项。修改规则文件不等于运行中的任务已重新加载，不得据此声称行为验证通过。
<!-- /codex-gpt6-working-agreements-v1 -->
